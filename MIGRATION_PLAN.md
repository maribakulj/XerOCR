# Plan de migration XerOCR — par tranches verticales

> **Nature** : plan d'**enveloppe** transverse (séquencement des tranches + invariants
> de contrat). Complète les guides par couche (`xerocr/**/MIGRATION_COUCHE_*.md`,
> `xerocr/**/ANALYSE_COUCHE_*.md`) en les **reliant** en un parcours de bout en bout.
> **À lire avec** [`CLAUDE.md`](CLAUDE.md) (le contrat de travail : deux axes, 5 garde-fous).
>
> **Statut épistémique** (`CLAUDE.md` §9) : l'**enveloppe** (contrats, séquencement) est
> **durable** ; la **surface** (quelle métrique / route / module exactement) est
> **PROVISOIRE — à confirmer au build**. Le contact du code amont non encore mergé prévaut.

---

## Tableau de bord (roll-up) — état vivant

> **Source de vérité du détail = la `DoD vivante` de chaque `COUCHE` doc.** Ce
> tableau n'est qu'un **index** : il pointe, il ne recopie pas (sinon il dériverait).

**Légende tri-état** : `[x]` fait **+ preuve nommée** (test/commande/grep) · `[ ]` à faire ·
`[~]` **différé/réserve avec raison** (distinguer *différé-par-design* de *réserve-ouverte*).

| Couche / Tranche | État | DoD détaillée (autorité) |
|---|---|---|
| 1 `domain` | ✅ **vert** ; **+`CanonicalLayout`** (T5, type pivot mise en page neutre) **+`PipelineStep.fanout`** (T5, drapeau déclaratif reconnaissance par région) | `xerocr/domain/MIGRATION_COUCHE_1.md` §DoD |
| 2 `formats` | ✅ **vert** ; **+`alto/layout_map`** (T5, `alto_to_layout` + `layout_to_alto` ↔ neutre) **+`pagexml/layout_map`** (T5, `page_to_layout`) | `xerocr/formats/MIGRATION_COUCHE_2.md` §DoD |
| 3 `evaluation` | ✅ **T2** (CER/WER/MER · stats `scipy` · `cross_engine`) **+ T7 philologie/profil** (`cer_diplo` · `diacritic_err` · `del_rate`/`ins_rate` ; `rapidfuzz` en prod) **+ T5 structure** (`region_cer` · **`region_detection`** F1 IoU coords-relatives · loader `LAYOUT` JSON+**ALTO**+**PAGE** · **projecteur `layout_to_text`** + exécution `ProjectionSpec` dans le runner) | `xerocr/evaluation/MIGRATION_COUCHE_3.md` §DoD |
| 4 `pipeline` | ✅ **T3** (Protocol + exécuteur · annulation câblée) ; **+`fanout`** (T5, reconnaissance par région + échec partiel) **+ exécuteur fan-out déclaratif** (`step.fanout` → `execute_region_fanout`, LAYOUT rempli persisté + estampillé) **+ `RegionCropper`** (T5, crop injecté → pipeline hybride seg→OCR par bloc) | `xerocr/pipeline/ANALYSE_COUCHE_4.md` §DoD |
| 5 `adapters` | ✅ **T3 complet** (`precomputed`+`tesseract`+`openai`+`ollama`) **+`llm/mistral`** (post-correction cloud, secret `MISTRAL_API_KEY`) ; **+`storage/JobStore`** (journal SSE) **+`storage/publisher`** (S3, push GitHub opt-in) ; **+`layout/precomputed`** (T5, source LAYOUT + recognizer région) **+`layout/assembler`** (T5, `AltoAssembler` LAYOUT→ALTO_XML) **+`layout/crop`** (T5, `crop_region` PIL — découpe réelle des blocs) **+`ocr/google_vision`** (Étape 2a parité moteurs, D-076 : cloud first-party REST `httpx`, extra `[google]`, clé `GOOGLE_VISION_API_KEY`, tarifé `cloud_page_kinds`) **+`ocr/azure_di`** (Étape 2b, D-077 : Azure Document Intelligence REST async POST→sonde, extra `[azure]`, `AZURE_DOC_INTEL_ENDPOINT`+`_KEY`, tarifé `cloud_page_kinds`) **+`ocr/pero`+`ocr/calamari`** (Étape 2c, D-078 : OCR/HTR **locaux** in-tree comme Kraken, extras `[pero]`/`[calamari]`, `model` requis, non déployés au Space ; `docs/PLUGINS.md` documente le seam tiers) **+ parsing jetons LLM testable** (Étape 2d, D-079 : `_completion_from_{chat,message}` purs extraits des clients live → cartographie usage→`tokens_in/out` couverte sur cassette ; zero-shot E2E) **+ `xerocr/prompts/`** (Étape 2e, D-080 : 16 prompts curés par période — donnée + loader dynamique, `Competitor.prompt_name` résolu au plan, `<select>` curé + textarea libre dans l'UI ; **clôt la parité moteurs**) | `xerocr/adapters/ANALYSE_COUCHE_5.md` §DoD |
| 6 `app` | ✅ **T2** (orchestrateur · loader/sécurité) ; **+`JobRunner`** (TU2.a, + hook publication S3) **+`CorpusStore`** (TU2.c) **+ builders segmentation** (T5 : `precomputed_layout` · `precomputed_region` · `alto_assembler`) **+ `discover_plugins`** (T6, entry-points `xerocr.modules`, fail-closed public) | `xerocr/app/ANALYSE_COUCHE_6.md` §DoD |
| 7 `reports` | ✅ **T2** + **design complet** (S4.a→S4.b : chrome · polices data-URI · readouts/data-bars · by-engine · by-document · crosses) **+ `synthesis`** (T7 : verdict factuel par vue — meilleur pipeline CER · Δ au suivant · significativité, gaté sur `cer`, déterministe) **+ compare client-side** (Étape 3a, D-081 : widget « comparer un run » dans le rapport autonome — `_assets/compare.js` inliné + données inertes + bandeau deltas CER ; CSP `/reports/` épingle le hash sha256) **+ badges moteur A→E** (Étape 3a, D-082 : `engine_badges.py` source unique lettre+accent cyclique, identité stable entre sections) **+ deeplinks + sommaire + ARIA** (Étape 3a, D-083 : régions ancrées `#r-<name>` + `nav` sommaire, ancres natives zéro JS) **+ navigation clavier + palette daltonien** (Étape 3a, D-084 : `report.js` j/k + `?palette=cb` ; `embedded.py` source unique scripts inlinés + hashes CSP) **+ drill-in diff GT↔hypothèse** (Étape 3b, D-085 : `text_diff.char_diff` surligne les pires lignes) **+ galerie de documents** (Étape 3b, D-086 : `gallery.py` cartes synthétiques + CER/badges sur la charte ; fac-similés réels = opt-in ultérieur) | `xerocr/reports/ANALYSE_COUCHE_7.md` §DoD |
| 8 `interfaces` | 🔨 **T4 socle ✅ · Space S1–S5 ✅ · S6 en cours** (vitrine · lanceur · persistance · vues rapport · sécurité publique ; importeurs `import/*` + pages `/history`/`/library` + form UI d'import + **page `/segmentation`** au design ; reste **Tranche 2** run de segmentation web) | `xerocr/interfaces/ANALYSE_COUCHE_8.md` §DoD |
### Les deux axes : **Moteur `T#`** & **Space `S#`** — un seul tableau, dépendances explicites

**Deux *natures* de travail, un seul tableau d'autorité.** `T#` = la
**bibliothèque déterministe** (couches 1–7, intérieur→extérieur). `S#` = l'**app
web interactive** (couche 8 + déploiement) qui **consomme** le moteur. Les arêtes
`⟸` sont des **dépendances** (un `S` ne peut aboutir que si son moteur est prêt) :
c'est un **ordre partiel**, pas deux files déconnectées.

| Axe **Moteur** `T#` | Livre | État |
|---|---|---|
| **T0** fondations | domain + formats | ✅ **clos** (§9) |
| **T1** squelette ambulant | `xerocr demo` octet-stable (3→8) | ✅ **fait** |
| **T2** axe texte | `tesseract` réel · CER/WER/MER · stats · `run`/`compare` | ✅ **fait** |
| **T3** OCR + LLM | pipeline 2 étapes · `openai`+`ollama` · annulation câblée | ✅ **fait** |
| **T4** socle web | `create_app` factory · package `security/` · vitrine **read-only** · Docker/Space | ✅ **fait** |
| **T5** structure / segmentation | `CanonicalLayout` · fan-out région · métrique `(LAYOUT,LAYOUT)` | 🔨 **squelette livré** (precomputed : LAYOUT régions-seules → fan-out → `region_cer` page → rapport, bout-en-bout) · **+ `alto_to_layout`** (GT layout ALTO réelle chargeable) · **+ assemblage `layout→ALTO_XML`** (`AltoAssembler`, contrat §3 bouclé) · **+ pipeline déclarative 3 étages** (une `PipelineSpec` : segment → fanout reconnaissance → assemblage, modules via factory) · **+ `page_to_layout`** (GT layout PAGE réelle chargeable ; sniff ALTO/PAGE) · **+ câblage données réelles** (4 producteurs réels chargés ; `region_cer` correct/non-trivial/non trompeur via le runner) · **+ projecteur `layout→texte`** (`ProjectionSpec` exécutée : un LAYOUT noté par les métriques texte ; **vrai benchmark BNL** ABBYY-vs-Tesseract, test `live`) · **+ `region_detection`** (F1 IoU de boîte, coords relatives mm10/px ; vrai F1 ABBYY-vs-Tesseract 0.35@0.5) · **+ pipeline HYBRIDE réel** (crop des blocs + OCR Tesseract par bloc sur vrai TIFF BNL ; `region_cer` hybride 0.157, test `live`) — *le cas d'usage central* · reste **segmenteur externe out-of-tree (= T6** : plug YOLO via entry-points + adapter) · `region_cer` apparié-IoU (optionnel) |
| **T6** extensibilité tierce | entry-points `xerocr.modules` + 1 plugin de réf. | 🔨 **découverte livrée** : `discover_plugins` (groupe `xerocr.modules` → builder tiers enregistré **comme le socle**, même `Module` Protocol) · **fail-closed en mode public** (pas de code tiers in-process sur serveur exposé) · plugin de référence (segmenteur tiers `IMAGE→LAYOUT`) · version tierce → `RunManifest.module_versions` (gratuit). Câblé CLI (activé) + web (désactivé public). **Preuve réelle** : vrai `EntryPoint` (résolution dotted-path) + **paquet pip-installable auto-découvert** via `importlib.metadata` (test `slow`, isolé en `--target`/process enfant) |
| **T7** surfaces internes | importeurs cœur · longitudinal · philologie · métriques | ✅ **scope listé livré** : 3 métriques — `cer_diplo` (#25) · `diacritic_err` (#26) · `del_rate`/`ins_rate` (#27), câblées sur le benchmark réel BNL · **+ `synthesis`** (section verdict factuel par vue, gatée `cer`, déterministe — ferme la dernière case S4) · **+ importeur IIIF** (1ᵉʳ importeur cœur : adapter `_http` anti-SSRF + parser manifeste v2/v3 → service `app` matérialise un `CorpusSpec` images-seules ; sortie unique `CorpusSpec`, transport durci, test loopback réel + `live` distant) · **+ importeur eScriptorium** (porteur de **GT** : auth token + pagination `next` + extraction couche transcription → `CorpusSpec` **scorable** avec `GroundTruthRef` RAW_TEXT ; prouvé par test loopback réel non-mocké : auth + pagination + GT) · **+ importeur Gallica** (images via manifeste **IIIF `/iiif/`** réutilisé + **OCR via ALTO** `RequestDigitalElement` étiqueté `gt_source=gallica_ocr` — type `REFERENCE_TEXT` D-053 ; **R-7 clos** — mapping vue→`f{n}` lu dans l'URL d'image, **2 bugs prod corrigés** (manifeste 403, `texteBrut` ALTCHA→ALTO, D-058), prouvé sur loopback réel + vraies pages Gallica ALTO) · **+ découverte HTR-United & HuggingFace** (catalogues : `fetch_catalogue` YAML + repli démo `is_demo`, `HuggingFaceCatalogue.search` socle référence + API best-effort `source` ; loopback réels) — **importeurs cœur COMPLETS** · **+ longitudinal** (`HistoryStore` SQLite : `add`→`history`→`regressions` direction-aware avec seuil ; `app/history` aplatit `RunResult`→lignes ; round-trip réel + idempotence + persistance testés) · **+ philologie `mufi_err`** (taux d'erreur sur caractères MUFI/médiévaux — PUA + abréviations latines + ſ/þ/ligatures ; alignement `rapidfuzz`, `None` si la réf n'en porte aucun, enregistré au socle) — **scope T7 listé complet** ; familles de métriques riches (NER/taxonomy/calibration) restent **additives** (enveloppe `RunResult` déjà dimensionnée) |
| **T8** consolidation parité (cf. [`PLAN_PARITE.md`](PLAN_PARITE.md)) | hygiène docs/annotations · **E1 ressources** (`StepOutput`/`ResourceUsage`, durées mesurées par l'exécuteur + jetons LLM → `RunResult.usage`, `schema_version=2`) · **E2 analyses** (contrat documenté, champ différé au 1ᵉʳ payload — D-066) | ✅ **fait** (D-066) — preuves : `tests/pipeline/test_executor.py::test_usage_records_duration_and_tokens` · `tests/app/test_orchestrator.py::test_run_result_carries_sorted_usage` · `tests/evaluation/test_result.py::test_usage_channel_round_trips` · `test_status_freshness` étendu à `NEXT_SESSION.md` |
| **T9** verdict statistique complet (PLAN_PARITE) | **Nemenyi post-hoc** (table Tukey/√2, interpolation + extrapolation conservatrice k>50) · **bootstrap IC** percentile graine fixe (stdlib `random`, pas de numpy) · **1ᵉʳ payload `analyses`** (`inference` : rangs moyens, CD, groupes d'ex-aequo, IC par pipeline ; k=2 → rangs+IC seuls, verdict = Wilcoxon) · section `cross_engine` enrichie (rangs/CD/groupes/IC, lecture seule) · `synthesis` : **règle anti-contradiction** (groupe Nemenyi ⊃ paire de tête → « égalité statistique », le corrigé l'emporte sur la p brute) + « écart confirmé » | ✅ **fait** (D-067) — preuves : `tests/evaluation/test_inference.py` (parité numérique vs source Picarones : CD 1.1715 · q 2.343 · IC (0.095, 0.11625)) · `tests/evaluation/test_runner.py::test_inference_analyses_through_evaluate_run` · `tests/reports/test_synthesis_section.py::test_nemenyi_tie_overrides_raw_significance` |
| **T10** économie (PLAN_PARITE) | table **`pricing.json`** datée (`valid_until`, JSON stdlib — `yaml` hors whitelist archi couche 3) · coût indicatif = temps machine (durée E1 × taux) + jetons cloud mesurés × €/MTok (modèle hors table → `None` + motif, **jamais un zéro silencieux**) · débit effectif (pages/h corrigé relecture, 5 s/erreur) · coût marginal vs le moins cher · **fronts de Pareto** cer×coût et cer×durée · payload `economics` (2ᵉ membre de l'union `analyses`, discriminée `kind`) · section `economics` (lecture seule, **avertissement de péremption** vs `completed_at` — déterministe) | ✅ **fait** (D-068) — preuves : `tests/evaluation/test_economics.py` (valeurs **dérivées à la main**, règle §5.8b) · `tests/reports/test_economics_section.py` · démo octet-stable préservée (`test_cli_demo`, canaux environnementaux filtrés au rendu démo) |
| **T11** diagnostic d'erreurs (PLAN_PARITE) | 2 scalaires au socle + vue `text` par défaut : **`searchability`** (recall de mots à Levenshtein ≤ 2 — la question des bibliothèques) · **`hallucination`** (trigrammes non ancrés — critique pour les pipelines LLM) · **3ᵉ payload `analyses` : `diagnostics`** (confusions de caractères par pipeline — segments `replace` difflib · **pires lignes** du corpus avec extraits verbatim tronqués · documents les plus difficiles par CER moyen), collecté **au fil du scoring** (mêmes représentations normalisées, zéro relecture) · section `diagnostics` (7ᵉ du socle, lecture seule, extraits échappés) | ✅ **fait** (D-069) — preuves : `tests/evaluation/test_diagnostics.py` (valeurs **dérivées à la main**, §5.8b) · `test_runner.py` (payload via `evaluate_run`) · `tests/reports/test_diagnostics_section.py` ; **différé** : sous-tranche philologie étendue (abréviations/typo early-modern) + blocs hallucinés détaillés — au prochain consommateur |
| **T12** confidences & calibration (PLAN_PARITE) | **`ConfidenceToken`** en domain (déclencheur backlog atteint : 1ᵉʳ consommateur) · tesseract produit le **sidecar `CONFIDENCES`** (`image_to_data`, conf 0-100 → [0,1], **best-effort** : échec → sidecar vide + warning, jamais une panne d'OCR) · **calibration ECE/MCE** (Guo et al. 2017 — formules publiées) + bins de fiabilité, « correct » = mot du jeton ∈ multi-ensemble des mots de la GT (proxy auditable documenté) · **4ᵉ payload `analyses` : `calibration`** · section `calibration` (8ᵉ du socle, lecture seule) | ✅ **fait** (D-070) — preuves : `tests/evaluation/test_calibration.py` (ECE/MCE **dérivés à la main**, §5.8b) · `tests/adapters/test_tesseract.py::{test_execute_writes_confidences_sidecar,test_confidences_failure_degrades_to_empty_sidecar}` · `tests/reports/test_calibration_section.py` ; **différé** : confidences Kraken (T14) · scalaire ECE en historique (consommateur à confirmer) |
| **T13** taxonomie d'erreurs (PLAN_PARITE) | classification par **règles pures** (aucun ML, aucun lexique) : `segmentation` (fusion/fragmentation au niveau segment) · `case` · `diacritic` (NFD) · `ligature` (œ/æ/ﬁ…) · `visual` (canon rn↔m, l/1/I, O/0, u↔n, ſ↔s — appliqué aux seules paires déjà fautives) · `lacuna` · `insertion` · `other` ; alignement mot-à-mot difflib · **5ᵉ payload `analyses` : `taxonomy`** (comptages + ordre canonique), collecté au fil du scoring (mêmes contextes texte que diagnostics) · section `taxonomy` (9ᵉ du socle : occurrences + parts) | ✅ **fait** (D-071) — preuves : `tests/evaluation/test_taxonomy.py` (chaque règle **dérivée à la main**, §5.8b) · `test_runner.py` · `tests/reports/test_taxonomy_section.py` ; **élagué** : classes à données externes (`hapax` lexiques · `oov_character` charset moteur · `abbreviation` table MUFI) — réintroduites si un consommateur apporte la donnée ; **différé** : T13.b NER (extra `[ner]`, spaCy) |
| **T14** moteurs HTR & cloud (PLAN_PARITE) | **Kraken first-party** (extra `[kraken]`, HTR — le moteur d'eScriptorium) : `IMAGE → RAW_TEXT + CONFIDENCES` (jeton/mot = moyenne des confiances de caractères), paramètre `model` (.mlmodel) **obligatoire** (pas de modèle embarqué) · **Mistral OCR** (cloud, extra `[mistral]` réutilisé, kind `mistral_ocr`) : markdown des pages, facturé **à la page** — `cloud_page_kinds` ajouté à la table de tarifs (0,92 €/1k pages) et au modèle de coût (T10) · builders + sondes `engine_statuses` + `_OCR_ENGINES` (CLI/web) | ✅ **fait** (D-072) — preuves : `tests/adapters/test_kraken.py` (jetons **dérivés à la main**) · `test_mistral_ocr.py` · `test_economics.py::test_per_page_cloud_kind_hand_computed` · `test_engines.py`/`test_module_registry.py` (matrice) ; **différé** : run `live` kraken sur fixture manuscrite (binaire+modèle requis) · **Calamari = plugin out-of-tree** (dépôt séparé, valide T6 — hors de ce dépôt par design) |
| **T15** reprise d'exécution (PLAN_PARITE — déclencheur levé par décision produit, D-073) | `app/resume.py` : **cache d'exécution adressé par empreinte** — clé SHA-256 = code_version + spec pipeline + adapter_kwargs + **contenu** de l'image (taille + SHA-256, pas de mtime) ; artefacts + `usage` d'origine persistés/rechargés ; **l'évaluation est toujours recalculée** (changer la GT re-note sans re-OCRiser) ; entrée corrompue → ré-exécution ; pas de journal NDJSON (l'adressage par contenu remplace l'invalidation) · orchestrateur `resume_store=` · CLI `run --resume-dir` | ✅ **fait** (D-073) — preuves : `tests/app/test_resume.py` (2ᵉ run = **zéro exécution**, invalidation par version/contenu, corruption tolérée, usage restauré) |
| **S7** surfaces utilisateur (PLAN_PARITE) | **S7.a livré** : export **CSV** tableur (`reports/csv_export.run_result_csv` — lecture pure, 1 fichier, colonne `scope` agrégat/document ; CLI `run --csv`) · **CLI `history`** (série d'un pipeline / régressions avec Δ signé — parité avec la page web) · endpoint **`GET /api/normalization/profiles`** lu **dynamiquement** depuis la couche 2 (jamais de liste statique — la leçon Picarones) | 🔨 **S7.a fait** (D-074) — preuves : `tests/reports/test_csv_export.py` (contenu exact) · `tests/interfaces/test_cli_history.py` · `test_engines_api.py::test_normalization_profiles_are_read_dynamically` ; **S7.b différé** : glossaire FR/EN (YAML élagué + bouton « ? ») · prompts de correction par période (données FR/DE + sélecteur) — prochaine session |

| Axe **Space** `S#` | Livre | Dépend de | État |
|---|---|---|---|
| **S1** coquille au design | rendu serveur · polices self-host · nav · FR/EN | T4 ✅ | ✅ **fait** |
| **S2** lanceur « Banc d'essai » | run de fond annulable · Moteurs `/api/engines` + page `/engines` · upload durci · gardes HTTP · SSE · page JS + **UI upload/sélection** | T2,T3,T4 ✅ | ✅ **fait** (run moteur réel = Space privé / test `live`) |
| **S3** persistance | push `RunResult` JSON → dépôt GitHub après run (opt-in secrets) | T4 ✅ | ✅ **fait** (best-effort · push réel = test `live`) |
| **S4** vues rapport au design | overview/by-engine/by-document/crosses/synthesis | couche 7 ✅ | ✅ **fait** : chrome + overview (S4.a) · polices data-URI (S4.b.1a) · readouts+data-bars (S4.b.1b) · by-document (S4.b.2) · by-engine (S4.b.3) · **crosses (S4.b.4)** · **synthesis** (verdict factuel, livré avec T7) — toutes les vues au design + rapport autonome octet-stable |
| **S5** sécurité publique | mode public · quotas · rate-limit · durcissement exposition | T4 ✅ | ✅ **fait** : mode public **enforced** (imports distants→403) · rate-limit durci (purge IP) · CSRF · CSP `'self'` · upload double-cap (413+422) ; **+ lanceur fail-closed** (D-075 : `PUBLIC_ENGINE_KINDS={precomputed,tesseract}` ; tout kind hors socle gratuit → **403 avant vérif clé/dispo**, cloud jamais tenté sur instance publique ; plugins gated) ; *multi-worker = limite documentée (Space mono-worker)* |
| **S6** surfaces UI | segmentation-UI · importeurs-UI | **T5** / **T7** (✅ débloqués) | 🔨 **en cours** : **S6.a importeurs distants** (IIIF · eScriptorium · Gallica → `POST /api/corpus/import/{iiif,escriptorium,gallica}`, même seam `CorpusStore.materialize`, CSRF, **gate mode public 403**, corpus run-sélectionnable) ✅ · **+ Historique** (recording `JobRunner`→`HistoryStore` après chaque run + `GET /api/history/{series,regressions}` lecture seule ; **ferme le différé D-042**) ✅ · **+ page `/history`** (rendu serveur : régressions + journal des mesures, FR/EN, nav vivante, 0 JS) ✅ · **+ page `/library`** (découverte HTR-United + HuggingFace : recherche, repli démo `is_demo`, FR/EN, 0 JS) ✅ · **+ S6.b form UI d'import** (section « importer un corpus distant » sur `/benchmark` : 4 sources IIIF/Gallica/eScriptorium/HuggingFace, JS léger CSRF → endpoints `import/*`, corpus importé = corpus actif du lancement ; masquée en mode public ; D-059) ✅ · **+ segmentation-UI squelette** (page `/segmentation` : SVG serveur des régions + table, `SegmentationStore` persistance LAYOUT + image, `GET /api/segmentation/{id}/image` anti-traversal, graine de démo bout-en-bout, FR/EN, 0 JS ; D-060) ✅ · **+ T2 sink LAYOUT** (orchestrateur `artifact_sink` + `JobRunner` persiste les `LAYOUT` produits au `SegmentationStore` ; `/segmentation` affiche `latest()` = run réel > démo ; un seul exécuteur, segmenteur-agnostique ; D-061) ✅ · **+ T2 run segmentation** (`POST /api/segmentation/run` lance `pp_doclayout` via le même `JobRunner` → sink → `/segmentation` ; `segmenter_statuses` catégorie séparée, gate 409, public-safe ; D-062) ✅ · **+ T2 bouton « Segmenter »** (sur `/benchmark` : déclenche le run sur le corpus courant, même SSE, lien vers `/segmentation` ; désactivé/motif si segmenteur indispo ; D-063) ✅ ; **boucle segmentation fonctionnellement complète** — reste seulement l'**image Space** (T2.5, **différée** : free-tier HF à valider ; D-064) · **+ remédiation post-audit** (D-065 : ISE HF réparés · couche 8 transport mince · **upload+imports déplacés `/benchmark` → Bibliothèque** + glisser-déposer · corpus sélectionné au Banc d'essai) · **+ image MOTEUR (OCR Tesseract gratuit sur le Space)** (D-075, [PLAN_FIN_MIGRATION Étape 1](PLAN_FIN_MIGRATION.md) : `deploy/Dockerfile.engine` bake Tesseract+`fra/lat/eng`, `OMP_THREAD_LIMIT=1`, smoke build fail-fast ; lanceur public fail-closed ; `RunManifest.system_binaries_lock` capté ; `deploy-space.yml` déploie le moteur + smoke OCR réel — **le segmenteur PP-DocLayout T2.5 reste différé**) |
| **Rapport autonome interactif** | HTML déterministe sans backend (artefact ≈ T1) | T1 ✅ | ✅ |

> **Alias historiques** (commits / anciens docs) : `TU1=S1` · `TU2=S2` · `TU3=S3` ·
> `TU4=S4` · `TU6=S5` · `TU5=T7` · `TU7=T5`. Le **statut fait foi ici** ;
> [`PLAN_SPACE_INTERACTIF.md`](PLAN_SPACE_INTERACTIF.md) = **spec UX**, sans roadmap.

**Règle anti-dérive (corollaire de « pas de consommateur = supprimé »)** : on ne
crée **pas** d'API/symbole public avant que **son consommateur de la même
sous-tranche** existe. Une API posée « en prévision » d'une sous-tranche ultérieure
est du spéculatif → élaguée à la revue (précédents : `blocked_cloud_kinds`,
`CLOUD_KINDS` dupliqué, retirés au commit `5c17cf2`).

### Rituel de réconciliation (NON négociable — c'est ce qui rend la redondance utile)

À chaque tranche, l'agent de construction :
1. **lit les deux** — ce plan (tranche + roll-up) **et** la `DoD vivante` de chaque couche touchée ;
2. **coche uniquement ce qu'un *gate nommé* prouve** (test/commande/grep) — jamais « je l'affirme » ;
3. **si pas coché → écrit pourquoi** (différé-par-design vs réserve-ouverte vs bloqué) ;
4. **si le plan et une DoD de couche divergent → arbitre** : corrige l'un, **ou** justifie l'écart dans le **journal de décisions** (§10) ;
5. **met à jour les deux dans le MÊME commit que le code** (règle d'or anti-dérive : les docs de Picarones mentaient parce qu'ils étaient mis à jour à part, ou jamais) ;
6. **n'ajoute les cases de *surface*** (quelle métrique/route exactement) **qu'au démarrage de la tranche** — pas toutes d'avance (l'enveloppe est détaillée maintenant, la surface est périssable).

---

## 0. Principes directeurs → mécanismes concrets

Les quatre exigences du projet ne sont pas des slogans : chacune est portée par un
mécanisme vérifiable, et **c'est elle qui justifie la découpe en tranches**.

| Principe | Mécanisme qui le garantit |
|---|---|
| **Simplicité** | 1 contrat de module (pas 4 registres) · 1 format de sortie `RunResult` (pas le double `BenchmarkResult`) · 1 signature de section · 1 pile HTTP. Chaque tranche **ajoute un seul concept**. |
| **Maintenabilité** | Budgets `<600 LOC` + `test_file_budgets` · garde-fous d'archi **dès la 1ʳᵉ tranche** · **zéro shim** · « pas de consommateur = supprimé » · golden **octet-stable** (déterminisme prouvé, pas espéré). |
| **Intelligence conceptuelle** | L'**enveloppe est dimensionnée plein-scope une fois** (axe 1, §1) : `RunResult` porte structure/NER/taxonomy **dès sa conception**, le registre est **générique sur `input_types`**. On coule des fondations pour 3 étages, on meuble un étage à la fois. |
| **Extensibilité (tranche→tranche & futur)** | **Un seul point d'extension** : le `Module` Protocol + entry-points `xerocr.modules`. Chaque tranche ne fait qu'**ajouter** (un module, une métrique, une section) sur des contrats **stables** → elle ne modifie jamais l'enveloppe précédente. **Inner→outer** : chaque tranche ne dépend que de couches finies. |

---

## 1. Invariants d'enveloppe — figés UNE fois, dès la tranche 1

Cœur de l'« intelligence conceptuelle » : ces contrats sont conçus **plein-scope
maintenant**, même remplis minimalement, pour que **rien d'aval ne force jamais leur
réécriture**. Une tranche ultérieure les **remplit** ; si elle doit les **modifier**,
c'est le signal qu'ils étaient mal dimensionnés → on s'arrête et on reconçoit.

| Pivot | Conçu pour (plein-scope) | Rempli en T1 (minimal) | Rend extensible |
|---|---|---|---|
| **`RunResult`** (`evaluation/result.py`) | CER/WER **+** taxonomy/NER/calibration/**structure** + par-doc + `schema_version` | 1 seul CER | toute métrique future = champ déjà prévu |
| **Registre type-driven** | sélection **100 % par `input_types`** | 1 métrique `(RAW_TEXT,RAW_TEXT)` | `(LAYOUT,LAYOUT)`, `(ENTITIES,…)` sans toucher le registre |
| **`Module` Protocol** (couche 4) | `name` · **`version`** · `input_types`/`output_types` · `execute(inputs,params,context,control)` | `precomputed` | tout module OCR/HTR/VLM/segmenteur/post-correcteur, **first-party ou tiers** |
| **`Protocol Section`** (`reports`) | 1 signature `render(RunResult, ctx) → Html\|None` + `requires` déclarés | 1 section overview/CER | toute section future, **sans data-layer** |
| **`ArtifactType.LAYOUT` + `region_id`** | réservés en couche 1 (déjà présents) | non utilisés | la tranche structure (fan-out région) **sans migration** |
| **Registre de modules + factory** (`app/modules/`) | résolution `name→Module` + **découverte entry-points** prête | 1 module en dur | plugins tiers (T6) |
| **`RunManifest`** (provenance câblée) | code_version + deps + binaires + hash params | rempli par l'executor dès T1 | reproductibilité de tout run futur |

---

## 2. Séquencement des tranches

```
T0  Fondations (domain + formats)                       [horizontal — vert, voir §9]
      │
T1  SQUELETTE AMBULANT  ── prouve l'enveloppe entière ──▶  xerocr demo
      │   3·4·5·6·7·8 · 1 CER · precomputed
T2  AXE TEXTE + 1er moteur réel ───────────────────────▶  xerocr run / compare
      │   tesseract · WER/MER · stats scipy · cross-engine · sécurité · repro
T3  OCR + LLM (post-correction) ───────────────────────▶  pipeline 2 étapes
      │   openai + ollama · 2e famille de module
T4  SOCLE WEB (couche 8 : create_app · security/ · vitrine READ-ONLY) ─▶ xerocr serve
─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
T5  STRUCTURE (segmentation / layout)  ★ ──────────────▶ métriques structurelles
      │   CanonicalLayout · fan-out région · segmenteur de référence   ──⟸ débloque S6-seg
T6  EXTENSIBILITÉ TIERCE  ★ ──────────────────────────▶  charger un module hors-arbre
T7  SURFACES INTERNES ──────────────────────────────────▶ importeurs cœur ──⟸ débloque S6-imp
          longitudinal · philologie · taxo · économie · métriques

   ════════════ AXE SPACE  S#  (app web · couche 8+déploiement · CONSOMME le moteur) ════════════
S1 coquille au design ✅           S2 lanceur (run/SSE/Moteurs/upload/gardes) 🔨  ⟸ T2,T3,T4
S3 persistance ⟸ T4               S4 vues rapport au design ⟸ couche 7
S5 sécurité publique ⟸ T4         S6 surfaces UI (segmentation/importeurs) ⟸ T5 / T7
```

Règle : chaque tranche est **fine mais de pleine profondeur** (tourne de bout en bout).
On n'ouvre l'axe **structure** (T5) qu'après avoir prouvé l'axe **texte** (T1-T3).
Aligné sur `MIGRATION_COUCHE_3.md` §15 (étend sa vue eval-centrée à serve / OCR+LLM /
importeurs / extensibilité / infra).

---

## 3. Détail par tranche

### T1 — Squelette ambulant

| | |
|---|---|
| **Objectif** | Prouver que **toute l'enveloppe s'emboîte** de bout en bout avec **une seule métrique scalaire**. Fin **et** de pleine profondeur. |
| **Traverse** | 3 (registre + `RunResult` + runner 1 métrique + `safe_*`) · 4 (`Module` Protocol + executor minimal) · 5 (`precomputed`) · 6 (orchestrateur minimal + `RunSpec` minimal + registre 1 module + provenance) · 7 (`Protocol Section` + `html` + 1 section) · 8 (CLI `demo`) |
| **Enveloppe figée** | les 7 pivots du §1 |
| **Surface minimale** | 1 module `precomputed` · 1 métrique `CER (RAW_TEXT,RAW_TEXT)` · 1 section overview/engines |
| **Élimine d'emblée** | double format (1 seul `RunResult`) · 4 registres→1 · narratif (jamais créé) · effets de bord d'import · data-layer |
| **Validation** | `xerocr demo --output r.html` → **HTML déterministe** · golden octet-stable · provenance dans `RunManifest` · import sans effet de bord |
| **Diffère** | tout moteur réel · stats · `CanonicalLayout` · web · multi-étapes |

### T2 — Axe texte + premier moteur réel

| | |
|---|---|
| **Objectif** | Un **vrai moteur** + la profondeur métrique texte + les stats + `compare`. **Tue la conversion double-format.** |
| **Traverse** | 3 (WER/MER + noyau texte **clés stables** + `profiles` + **scipy** + `cross_engine`→`RunResult`) · 5 (`tesseract`+`confidences`, utils racine) · 6 (orchestrateur réel + loader YAML + **sécurité chemins** + capture deps/binaires) · 7 (sections engines/cross + `compare.py`) · 8 (`run` + `compare`) |
| **Élimine** | conversion `RunResult→BenchmarkResult` supprimée · sécurité chemins fidèle et testée d'abord · provenance câblée · `_retry` unifié (bug jitter corrigé) |
| **Validation** | parité métriques vs `jiwer` · **run rejouable depuis le `RunManifest` seul** · entrées dégénérées→`None` · golden `RunResult` |
| **Diffère** | LLM · web · structure · importeurs réseau |

### T3 — OCR + LLM (post-correction)

| | |
|---|---|
| **Objectif** | Prouver les **pipelines multi-étapes** + une **2ᵉ famille de module** sur le même `Protocol`. |
| **Traverse** | 5 (`llm/base` splitté + `openai` + `ollama`) · 4 (binding `OCR→LLM`, executor multi-step) · 6 (`make_ocr_llm_pipeline_spec` mode `text_only`) · 8 (`run` pipeline) |
| **Élimine** | triple alias `PipelineMode`→1 · `llm/base` >400 splitté · `tokens_used` peuplé ou supprimé · annulation `control` câblée (ollama = référence) |
| **Validation** | `tesseract → llm text_only` produit du `CORRECTED_TEXT` non vide (bug historique Picarones) · le 2ᵉ module passe le **même** contrat sans cas particulier |
| **Diffère** | `zero_shot`/VLM (**0 consommateur** → non porté) |

### T4 — socle web (couche 8, **read-only**)

| | |
|---|---|
| **Objectif** | Poser l'**enveloppe de transport** et **servir** les rapports : la base sur laquelle l'axe **Space `S#`** se construit. |
| **Traverse** | 8 (`create_app()` factory · routers `home`/`reports` · package `security/` : CSP/rate-limit/en-têtes · chemins validés) |
| **Élimine** | effets de bord d'import (factory) · sécurité éclatée 7 modules→1 package |
| **Validation** | factory (instances neuves) · CSP stricte · `..`/symlink→404 · `serve` fumé via uvicorn · Docker/Space déployable **sans clé** |
| **Diffère** | tout l'**interactif** → axe **Space `S#`** (lanceur, jobs, SSE, upload, mode public) |

### Axe Space `S#` (app web interactive · couche 8 + déploiement) — détail

> Le Space **consomme** le moteur (`⟸`). Statut dans le tableau « Les deux axes »
> du roll-up ; `PLAN_SPACE_INTERACTIF.md` = spec UX des écrans.

| `S#` | Livre | ⟸ | Élimine / Valide | ex-TU |
|---|---|---|---|---|
| **S1** | coquille au design (Jinja + CSS, polices self-host, nav, FR/EN) | T4 | SPA lourde → rendu serveur ; CDN → self-host | TU1 |
| **S2** ✅ | lanceur : `JobRunner`+`JobStore` (annulation), `/api/engines` + page `/engines`, upload ZIP durci, gardes HTTP, SSE+`Last-Event-ID`, page JS `/benchmark` + **UI upload/sélection moteur** | T2,T3,T4 | 2 JobStore→1 (SSE réabsorbé) · annulation réelle · CSRF/mode public/zip-bomb **verts d'abord** ; run moteur réel = test `live` | TU2 |
| **S3** ✅ | persistance : `ResultPublisher` (couche 5) pousse le `RunResult` JSON vers un dépôt GitHub (API *contents*, `urllib`) après un run réussi — **opt-in via secrets** (`XEROCR_PUBLISH_REPO`/`_TOKEN`), **best-effort** (un échec ne fait pas échouer le run), jeton jamais journalisé ; `published_url` remonte sur le `Job`. Push réel = test `live` | T4 | disque HF éphémère → push durable ; la vitrine **rend** le JSON (pas besoin de pousser le HTML) | TU3 |
| **S4** ✅ | vues rapport au design : **S4.a** chrome rapport (gris chaud · en-tête pilule · cartes `sec` · tables tabulaires → autonome + octet-stable) + overview restylé ✅ · **S4.b.1a** polices du design **incorporées en data-URI** (Fluxisch Else + OCR-A, `reports/_style`) ✅ · **S4.b.1b** overview enrichi (**readouts** de portée + **data-bars** relatifs à la colonne) ✅ · **S4.b.2** section **par-document** (surface `RunResult.documents` — déjà calculé, jamais rendu — doc×pipeline groupé + data-bars) ✅ · **S4.b.3** **par-moteur** (classement trié + dispersion par-doc) ✅ · **S4.b.4** **crosses** au design (clé éclatée Vue/Métrique + verdict factuel) ✅ ; **synthesis** différé T7 | couche 7 | data-layer → lit `RunResult` direct | TU4 |
| **S5** | sécurité publique : mode public · quotas · rate-limit · durcissement | T4 | exposition non bornée | TU6 |
| **S6** | surfaces UI : segmentation-UI ⟸ **T5** · importeurs-UI ⟸ **T7** | T5 / T7 | — | TU7 / TU5 |

### T5 — Structure (segmentation / layout) ★ ambition nouvelle

| | |
|---|---|
| **Objectif** | **Matérialiser** ce que l'enveloppe réservait depuis T1. La **preuve que le dimensionnement précoce a payé** — zéro réécriture des couches 1-4. |
| **Traverse** | 1 (`CanonicalLayout` + parties en `domain`) · 2 (mappers `alto/page→layout`) · 3 (projecteur `layout→text` + **shapely** confiné + `region_detection`/`line_detection`/`reading_order`/`geometry_coverage`) · 4 (**fan-out** : segmentation → reconnaissance par région → réassemblage + routage par type de bloc) |
| **Ambition** | `segmentation (IMAGE→LAYOUT) → reconnaissance par région → assemblage (LAYOUT→ALTO)` + segmenteur de référence (starter pack) |
| **Validation** | `region_id` exercé · `RunResult` accueille une métrique `(LAYOUT,LAYOUT)` **sans modif de contrat** · golden **ALTO+PAGE** · round-trip fidélité |
| **Test conceptuel** | si T5 force à changer `RunResult`/registre/`Module`, le dimensionnement de T1 était faux. Attendu : elle ne fait qu'**ajouter**. |
| **Guide de portage** | `xerocr/pipeline/ANALYSE_T5_SEGMENTATION.md` (analyse durable Picarones + design cible PROVISOIRE + squelette + risques) |

### T6 — Extensibilité tierce ★ ambition nouvelle

| | |
|---|---|
| **Objectif** | **Ouvrir** le point d'extension unique : charger un **module hors-arbre** sans forker. |
| **Traverse** | 6 (`app/modules/` : découverte **entry-points `xerocr.modules`** + `register()` local + capture de **version** plugin → `RunManifest`) · 8 (mode public **désactive** la découverte : in-process = sécurité) |
| **Périmètre STRICT** | seules les **briques de pipeline** sont pluggables. Métriques/importeurs/sections/projecteurs/stats restent **first-party**. |
| **Validation** | 1 **plugin de référence** out-of-tree chargé/exécuté via le **même** `Protocol` · sa `version` remonte au `RunManifest` · en mode public, découverte **fail-closed** |

### T7+ — Surfaces incrémentales (1 concept par tranche, avec consommateur réel)

| Tranche | Ajout | Élimine au passage |
|---|---|---|
| **Importeurs** (IIIF→Gallica→eScriptorium→HTR-United→HF, 1/tranche) | sortie **unique `Corpus`** ; orchestration disque en `app` | bugs latents `Corpus(source=)`/`selected_indices+1` corrigés + test `live` · `_fallback_log` supprimé · **données démo en dur→`data/`** · dict-manifestes→`Corpus` |
| **Longitudinal** | store **tidy** en `adapters/storage` + section history | `history` SQLite changé de couche |
| **Philologie / Taxonomie / Économie / Image** | 1 métrique + 1 section par tranche | métriques mortes/doublons non portées · clés stables |
| **Observabilité / RGPD / Docker / Release** | `/metrics` opt-in · purge RGPD · Dockerfile · CI release | scripts sprint morts · `docs/archive` non migrés |

---

## 4. Matrice — chaque problème Picarones → la tranche qui l'élimine

| # | Problème Picarones | Éliminé en | Comment |
|---|---|---|---|
| F1 | Double format `BenchmarkResult↔RunResult` | **T1**→**T2** | 1 seul `RunResult` dès le départ ; conversion supprimée |
| F2 | Deux chemins de rapport + data-layer | **T1** | `Protocol Section` lit `RunResult` direct |
| F3 | 4 registres parallèles | **T1** | 1 registre type-driven |
| F4 | Code mort (`cache.py`,`yaml_io.py`,½ partial_store) | **jamais porté** | « pas de conso = supprimé » |
| F5 | 424 annotations de sprint | **transverse** | « garder le pourquoi, jeter la datation » |
| F6 | Fichiers >600 LOC | **transverse** | `test_file_budgets` |
| F7 | Moteur narratif | **jamais porté** | supprimé (D2) |
| F8 | 2 JobStore (SSE perdu) | **S2** | 1 store + SSE réabsorbé avant suppression legacy |
| F9 | Bugs latents masqués par mocks | **T7 importeurs** | corrigés + test `live`/`network` |
| F10 | Provenance dormante | **T1** | câblée dans l'executor |
| F11 | Annulation/deadline non câblées | **T3**→**S2** | `RunControl`/`Deadline` réels de bout en bout |
| F12 | Effets de bord d'import | **T1/T4** | `__init__` minces · `create_app()` factory |
| F13 | Données fabriquées en dur (~220 LOC) | **T7 importeurs** | `data/*.yaml` + `is_demo` |
| F14 | Sécurité éclatée (7 modules) | **T4** | 1 package `security/` |

→ 100 % des problèmes audités sont adressés, chacun à une tranche nommée.

## 5. Matrice — chaque ambition XerOCR → la tranche qui la matérialise

| Ambition (`CLAUDE.md`) | Enveloppe dès | Matérialisée en |
|---|---|---|
| Banc déterministe/reproductible + rapport factuel | T1 | T1 → T2 |
| **Extensibilité par modules tiers** | T1 (Module+registre) | **T6** (entry-points + plugin de référence) |
| **Segmentation/layout + fan-out région** | T1 (`LAYOUT`+`region_id`) | **T5** |
| Starter pack (precomputed/tesseract/openai/ollama/segmenteur) | T1 | T1→T2→T3→T5 |
| Sécurité in-process (mode public) | T1 | S5 + T6 |
| Anti-hallucination / what-if = sélection jamais re-mesure | T1 (`RunResult` par-doc) | tranche tardive (§8) |

---

## 6. Comment chaque tranche facilite la suivante

- **T1 fixe l'enveloppe** → T2…T7 n'**ajoutent** que de la surface sur des contrats stables : aucune ne réécrit une couche déjà faite.
- **Inner→outer** → quand on écrit T_n, ses dépendances (couches plus internes) sont finies et testées : pas de blocage « vers l'avant ».
- **Registre générique sur `input_types`** → ajouter une métrique = une fonction + une fiche ; le registre ne change pas. Idem `Protocol Section`.
- **`Module` Protocol unique** → ajouter un moteur (T2/T3/T5) **ou** un plugin tiers (T6) = la **même** opération. T6 ne fait qu'**exposer** la prise que T1 a conçue.
- **`RunResult` plein-scope** → une métrique structurelle (T5) atterrit dans un champ **déjà prévu** ; le rapport la lit sans data-layer.

---

## 7. Garde-fous transverses (actifs à chaque tranche)

1. **Tests d'archi dès le 1ᵉʳ commit de code de la tranche** : `layer_dependencies`, `no_legacy_imports`, **`no_side_effect_imports`**, **`file_budgets`**, `no_broad_except`, `single_version_source` (+ `no-orphan métrique↔section` dès T1).
2. **Budgets `<600 LOC`** ou entrée justifiée.
3. **Zéro shim, rupture nette** : un seul chemin, jamais d'ancien gardé « le temps de migrer ».
4. **« Pas de consommateur = supprimé »** : aucune surface spéculative.
5. **Golden déterministe refait** (jamais hérité de Picarones — incompatibilité numérique assumée, `MIGRATION_COUCHE_2.md` MIG-2).
6. **Une tranche = livrable end-to-end vert** (mypy + ruff + pytest) avant la suivante.

---

## 8. Points de décision ouverts (à trancher au build)

| Décision | Tranche | Options |
|---|---|---|
| Mécanisme what-if (rapport filtrable) | tardive | (a) runner pré-calcule → rapport sélectionne *(recommandé)* ; (b) ré-agrégation client |
| `control`/deadline : honorer vs best-effort | T3/T4, **par adapter** | trancher cas par cas, ne pas traîner un param non câblé |
| Placement `corpus_service` (ZIP+appariement) | T7 importeurs | `app` vs `adapters/corpus` |
| Forme exacte de `CanonicalLayout` | T5 | à confirmer **avec son 1ᵉʳ consommateur**, pas avant |
| Pile HTTP unique (`httpx`) vs dual SDK/REST | T2/T7 | `httpx` pour auth simple ; SDK gardé pour Google/Azure |

---

## 9. État vérifié de T0 (fondations) — vert, mais pas « clos »

Mesuré le 2026-05-31 sur la branche de travail :

| Gate | Résultat |
|---|---|
| `pytest tests/` | **158 passed**, **95,14 %** coverage |
| `mypy --strict -p xerocr.domain` / `mypy -p xerocr.formats` | **Success, 0 issue** |
| `ruff check xerocr/ tests/` | **All checks passed** |
| DoD couche 1 — symboles interdits | **absents** (`PicaronesError`/`BaseModule`/`Fact`/`LEGACY_VALUE_ALIASES`/`pipeline_names`) |
| `ArtifactType.LAYOUT` + `Artifact.region_id` | présents (`xerocr/domain/artifacts.py`) |
| Tests sécurité XML + ordre de normalisation | présents (`tests/formats/test_xml.py`, `…/text/test_normalization_edges.py`) |
| Marqueurs d'inachevé (`TODO`/`NotImplementedError`/`...`) | **aucun** dans `domain`+`formats` |

**Réserves à lever avant de déclarer T0 « clos » (≠ différés par design) :**

1. **2 garde-fous d'archi manquants** : `tests/architecture/` ne contient que 4 tests ;
   **`test_no_side_effect_imports`** (exigé `MIGRATION_COUCHE_1.md §7`) et
   **`test_file_budgets`** (`CLAUDE.md §5`) sont **absents**. L'import est propre
   aujourd'hui, mais rien n'empêche une régression.
2. **`CLAUDE.md §0` périmé** : annonce « dépôt vierge… aucune couche implémentée »,
   branche obsolète. À corriger (induit en erreur une session fraîche).
3. **Types domain sans consommateur** (`evaluation.py`) : **réserve levée** —
   `MetricSpec`/`EvaluationView`/`EvaluationSpec` sont consommés par le runner et le
   builder de specs (`evaluation/runner.py`, `interfaces/web/routers/runs.py`,
   `interfaces/demo.py`) ; `ProjectionSpec` par les projecteurs (`evaluation/projectors.py`
   + `runner._context_for`, T5/D-031 ; + `identity_text` du Lot C/D-053). Plus aucun
   type `domain` orphelin.

**Différés par design (NON des manques)** : backlog domain (`RunSpec`/`ProjectionReport`/
`ConfidenceToken`), `CanonicalLayout`, l'assembleur — reportés à leur tranche
(anti-spéculatif).

---

## Cibles de distribution (calcul vs vitrine)

> **Principe :** séparer le **calcul** (produire des résultats — exige moteurs/clés) de la
> **vitrine** (montrer des résultats — n'exige rien). L'archi (déterminisme + `RunManifest` +
> adapter `precomputed` + rapport HTML autonome) est faite pour ce découplage.

| | **Calcul** | **Vitrine** |
|---|---|---|
| Où | en **local** (`serve`/CLI) : tes clés, tes corpus, privé | **hébergée**, partageable |
| Clés | oui (les tiennes, sur ta machine) | **aucune** déployée |
| Coût / abus | tu ne paies que tes runs | nul (rien à calculer) |
| Quoi | moteurs OCR/LLM | **rapport HTML autonome, interactif côté navigateur** (repli/tri/filtre/comparaison de runs **déjà calculés** ; jamais de re-mesure) |

**Canaux, par engagement croissant — le Space n'est PAS optionnel :**

1. **Rapport autonome interactif** — livrable **≈ T1**. HTML déterministe, sans backend, sans clé, sans CDN ; interactif dans le navigateur (sélection/exploration, pas de recalcul). **Premier artefact partageable**, pas un substitut du Space.
2. **Space hébergé « vitrine » (mode public)** — **cible engagée, axe Space `S#`** (sur le socle T4). Sert tes runs pré-calculés via l'app complète ; allowlist **fail-closed** (moteurs à clé bloqués) ; **zéro secret déployé**. La démo publique.
3. **BYO-key par *duplication*** — **quasi-gratuit si (2) est bâti propre** (config-par-secrets, boot-sans-secret, fail-closed) : activer = surtout **documenter** le flux *Duplicate this Space* (chaque user met SA clé dans SA copie). La **custodie de secrets reste hors de ton assiette**. Coûts : friction (compte HF + hardware) et forks qui vieillissent (pas de push de fix ; `RunManifest` rend la divergence détectable).
4. **Service partagé BYO-key** (une boîte, clés saisies chez toi) — **non retenu par défaut** : custodie de secrets + limites anti-abus + hygiène **testée** (DoD couche 8). À n'ouvrir que sur besoin explicite.

**Écarté :** desktop bundlé PyInstaller (D-006) — « install facile » = vitrine + `pipx`/`serve`.

**Conséquences de conception (couche 8) :** la vitrine est **duplicable par construction** dès
sa v1 ; et comme **ton code tourne avec la clé d'autrui en mode dupliqué**, l'hygiène des clés
(mémoire-seule, jamais journalisée/persistée/rendue) est un invariant **testé**, pas une intention.

---

## 10. Journal de décisions (ADR-lite, append-only)

> Toute décision/arbitrage qui **confirme, corrige ou contredit** un verdict
> PROVISOIRE d'un `COUCHE` doc, ou qui ajoute un choix transverse, est tracée ici.
> « Expliquer tous les choix faits » — garder le *pourquoi*, dater l'entrée.

| ID | Date | Tranche | Question / arbitrage | Verdict | Pourquoi | Remplace quel PROVISOIRE |
|---|---|---|---|---|---|---|
| D-001 | 2026-05-31 | T0 | `model_rebuild()` au niveau module est-il un effet de bord interdit ? | **Allowlisté** dans `test_no_side_effect_imports` | Idiome Pydantic v2 pur/idempotent/requis pour modèles récursifs (régions imbriquées) ; ≠ `register_default_metrics()` | — (découvert en calibrant le gate) |
| D-002 | 2026-05-31 | T0 | 2 garde-fous d'archi manquants | **Ajoutés** : `no_side_effect_imports` + `file_budgets` | Exigés par `MIGRATION_COUCHE_1 §7` + `CLAUDE.md §5` ; étaient absents → régression possible | comble la DoD couche 1 |
| D-003 | 2026-05-31 | transverse | Un agent affirmait « 27 violations d'archi en `interfaces` » | **Réfuté** : ce sont des imports légaux (couche plus interne) | `test_layer_dependencies` l.258 « peut importer plus interne » ; CI verte. C'est une **convention** (le run passe par `app`), pas une violation dure | crée la convention L7 (plan §8) |
| D-004 | 2026-05-31 | méthode | Où vit la DoD vivante ? | **Dans chaque `COUCHE` doc** + plan = roll-up/orchestration/journal | Redondance plan↔couche = **détection d'erreur** (réconciliation forcée), pas duplication ; les deux sont complémentaires | — |
| D-005 | 2026-05-31 | méthode | Granularité des cases par tranche | **Enveloppe + garde-fous + validation** | Signal porteur sans bruit (≠ case par fichier) | — |
| D-006 | 2026-05-31 | distribution | Comment partager / qui peut l'utiliser ? | **Vitrine hébergée non-optionnelle (T4), duplicable par construction** ; desktop écarté | Découpler calcul (local, tes clés) / vitrine (sans clé) ; BYO-key par *duplication* HF = doc, pas un chantier (custodie de secrets hors de ton assiette) ; rapport autonome **interactif** = artefact T1 | crée §Cibles de distribution |
| D-007 | 2026-05-31 | T1 | Outillage métrique & CLI (deps minimales) | **CER = impl maison déterministe** (jiwer = oracle de parité à T2) ; **CLI = argparse** (stdlib) | `pyproject` n'a ni `jiwer` ni `click` ; squelette **sans dépendance ajoutée** + contrôle du déterminisme ; cadre le T2 « parité vs jiwer » | — |
| D-008 | 2026-05-31 | T1 | `RunControl` : `raise_if_cancelled` à garder ou retirer ? | **Conservé** (et devient le mécanisme d'annulation coopératif) ; `register_cancel_handle` **omis** (0 conso) | L'analyse couche 4 disait « retirer, 0 conso » — **corrigé** : en T1, executor + `precomputed` l'appellent (tests verts), donc il A un consommateur. Le handle SDK, lui, reste omis jusqu'à l'adapter LLM (T3/T4) | corrige verdict ANALYSE_COUCHE_4 (`run_control`) |
| D-009 | 2026-05-31 | T1 (couche 3) | Réserve T0 : `EvaluationSpec`/`ProjectionSpec` sans consommateur | `MetricSpec`/`EvaluationView`/`EvaluationSpec` **confirmés** (registre + runner les consomment) ; `ProjectionSpec` **différé** (T2, projections) | T1 leur donne enfin un consommateur → lève la tension « pas de conso = supprimé » pour les 3 ; `ProjectionSpec` attend sa tranche | lève partiellement la réserve couche 1 §8 |
| D-010 | 2026-05-31 | T1 (couche 6) | `RunSpec` : reprendre le `StepSpec` de l'héritage ? | **Non** — `RunSpec` compose `PipelineSpec`/`PipelineStep` (domain) directement ; **pas de `StepSpec`** | Un `StepSpec` distinct = **2ᵉ représentation de pipeline** (la dette qu'on tue) ; `PipelineStep` déclaratif suffit. La factory résout `<kind>:<label>` via un **builder enregistré** (pas d'import de chemin pointé) | raffine le backlog CLAUDE (`RunSpec (+StepSpec)`) |
| D-011 | 2026-05-31 | T1 (clôture) | Golden du rapport : snapshot stocké ou déterminisme par construction ? | **Déterminisme par construction** : la section overview ne rend **ni timestamp ni chemin ni version** → HTML naturellement octet-stable | Un snapshot HTML stocké casse à chaque retouche CSS (fragile) ; « 2 runs == identiques » tient l'invariant §12 sans ce fardeau | clôt T1 (`xerocr demo` octet-stable) |
| D-012 | 2026-05-31 | revue T1 | Frontière de déterminisme : timestamps wall-clock (provenance, manifest, `run_id` par défaut) | **Métadonnée EXCLUE de l'identité** ; jamais rendue dans `RunResult`/HTML (vérifié : demo octet-stable) | §12 = « hash identique » (content_hash déterministe) ; `ProvenanceRecord.is_compatible_with` exclut déjà le timestamp ; cohérent avec `RunManifest` | guide le cache/store T2+ : clé = content_hash, **pas** `model_dump_json` |
| D-013 | 2026-05-31 | revue T1 | Revue complète (3 angles : ligne-à-ligne, inter-fichiers/déterminisme, qualité/altitude) | **0 bug T1** ; robustesse différée | Code déjà passé ruff/mypy/pytest. Sans conso T1 (pas de spéculation) : validation module↔step + `inputs_from` au **loader YAML (T2)** ; précision `run_id` au **store (T4)** ; I/O texte dédupliqué si chaud. **+ corrige** : `make type` ne couvrait pas les couches T1 → `mypy -p xerocr` | trace l'audit + ses suites |
| D-014 | 2026-05-31 | T3 | `register_cancel_handle` : distinguer une **annulation** d'une vraie panne réseau ? | **Sonder `is_cancelled` après l'échec** (`_fail_or_cancel`) ; mécanisme thread-safe (`Lock`) livré **avec son 1ᵉʳ consommateur** (`ollama`) → clôt le différé D-008 | L'implémentation source **devinait par le message** d'exception (fragile, dette D-A) ; sonder l'état est fiable. Le sondage coopératif (`raise_if_cancelled`) reste la **garantie** ; le handle est best-effort (un `cancel_event` partagé `set()` hors `trigger_cancel` ne déclenche pas les handles — limite assumée, documentée) | clôt D-008 (handle différé) + dette D-A (couche 5) |
| D-015 | 2026-05-31 | réconciliation T3 | Le roll-up (index) marquait encore **T1** pour les couches 3-8 alors que T2/T3 avaient shippé | **Index resynchronisé** sur les DoD par couche (autorité) : 3→T2, 4→T3, 5→T3 complet, 6→T3, 7→T2, 8→T2 ; lignes tranches T2/T3 ajoutées | Le rituel de réconciliation a **détecté une dérive réelle** (DoD par couche à jour, index en retard) — précisément ce que la redondance D-004 doit attraper ; corrigé dans ce commit | applique le rituel §Réconciliation |
| D-016 | 2026-05-31 | audit T2/T3 | Audit impitoyable de T2/T3 : 1 bug HIGH + 5 MEDIUM. Que corriger, et comment trancher les 2 choix de méthode ? | **HIGH** : contamination inter-pipelines (workspace partagé) → **isolation par sous-dossier**. **MEDIUM** : agrégat macro→**micro** (Σerr/Σpoids, macro reconstructible) ; `_MIN_SUPPORT` 2→**6** (plancher de puissance) + `ValueError→None` ; **`module_versions`** au manifeste (R-2) ; robustesse (GT `must_exist`, `OSError→EvaluationError`) ; parité MER **verrouillée** sur cas ambigu + honnêteté du docstring | Le bug HIGH brisait l'usage cœur (comparer 2 pipelines). Micro = la métrique conventionnelle, comparable à `jiwer`-corpus, et **macro reste dérivable** du détail par-doc → on garde les deux sans gonfler l'enveloppe. Plancher 6 : sous lui un Wilcoxon ne **peut** pas être significatif → `None` plutôt qu'un faux verdict. Parité MER exacte avec jiwer (tie-break d'alignement) = **non poursuivie** (coût/risque) : comportement déterministe pinné, divergence documentée | clôt l'audit ; `safe_mean` supprimé (0 conso) ; binaire tesseract→`system_binaries_lock` reste différé (live) |

| D-017 | 2026-05-31 | audit T2/T3 (LOW) | Balayage des 6 findings LOW + revue du balayage lui-même | CLI : erreurs métier/E-S → message + code 1 (plus de trace nue) · `_candidate_for` : **précédence explicite** (corrigé > brut), plus l'ordre alphabétique d'enum · `compare` : libellé Δ nommant les taux d'erreur (CER/WER/MER) · stem de fichier **injectif** via encodage URL · `expanduser` mort retiré (sécurité) · `--json` non-octet-stable = **laissé tel quel** (correct par D-012) | La **revue d'après** a rattrapé un **vrai bug que j'avais introduit** : l'échappement maison du stem (`_→__` puis `/→_`) collisionnait `a/_b` et `a_/b` → remplacé par `quote()` réversible (test de non-collision ajouté). Confirme que « corriger puis re-réviser » attrape ce qu'une seule passe rate | clôt le balayage LOW ; valide le rituel de revue |
| D-018 | 2026-06-01 | S4.b (pré-requis design) | `design/` empilait **3 générations contradictoires** (① polices CDN Bricolage/IBM Plex/Apfel · ② branding « Picarones » · ③ spec XerOCR) ; les `screenshots` étaient figés en ①+② (**sans la trame de points**) et les CSS se contredisaient (`--display` = Bricolage *vs* OCR-A selon le fichier) | **Consolidé en UNE source = ③** : **Fluxisch Else** (titres) + **OCR-A** (corps/données) **self-hosted**, `@font-face` + variables `--display/--sans/--mono/--serif` dans **`tokens.css` seul** (`picarones.css` hérite), branding **XerOCR**, **trame Xerox** confirmée. **Supprimé** : 5 mockups CDN, `design-canvas.jsx` (canvas orphelin), 5 screenshots périmés. **Ajouté** : `design/render/` (harnais offline reproductible, zéro réseau au rendu) → `screenshots/report-{overview,by-engine,by-document,crosses}.png` régénérés | Le garde-fou S4.b « comparer au `design/screenshots` » reposait sur une **référence fausse** (polices CDN, Picarones, sans trame). Source unique ⇒ changer la typo = **1 endroit** (trivial) et rendu **déterministe/offline**. Confirme la résolution typo de `design/README` | prépare la **décision police du rapport autonome** (S4.b) : **incorporer les woff2 en data-URI** (identité Fluxisch Else + OCR-A **et** autonomie — option (a) du brief) |
| D-019 | 2026-06-01 | S4.b.1a | Police du rapport autonome (décision **préparée par D-018**) : **système**, **`/static`**, ou **incorporée** ? | **Option (a) — woff2 incorporés en data-URI** : `reports/_assets/*.woff2` (couche 7, **sans dépendre de la couche 8**) ; `_style.font_face_css()` les lit → base64 → `@font-face data:` préfixés au CSS (Fluxisch Else Regular+Bold + OCR-A, 166 Ko). Titres **Fluxisch Else**, corps + données **OCR-A** | **Système** = identité partielle (perd les polices du design) ; **`/static`** = perd l'autonomie du *standalone* (le rapport doit s'ouvrir hors serveur). data-URI garde **identité + autonomie + octet-stabilité** (base64 d'octets figés = déterministe ; rapport 4,4→227 Ko, prix assumé) | a révélé un **test fragile** : `assert "WER" not in html` collisionnait le base64 → durci en `"<p>WER</p>"` (markup réel). reste **S4.b.1b** readouts/data-bars + overview riche |
| D-020 | 2026-06-02 | corpus réel (BNL) | Brancher un **vrai** corpus patrimonial (la démo n'avait que 3 docs synthétiques) + autant de moteurs accessibles que possible ? | **Mini-GT BNL** (presse luxembourgeoise XIXᵉ, **multilingue** allemand Fraktur + français, données ouvertes) en **fixture déterministe** `tests/fixtures/reference_corpus/bnl_mini/` : **30 docs**, GT extraite des ALTO v4 BNL + sorties OCR **figées** rejouées via `precomputed` → **CI-safe sans moteur**. **5 moteurs** : 4 Tesseract (`frk` Fraktur legacy · `deu` allemand · `fra` français · `deu_latf` Fraktur LSTM best) **+ `easyocr`** (deep-learning, autre architecture). Builder + 3 tests (fixture `module` = build unique), 2 vues (`text`/`caseless`). | Verdict XerOCR sur **données réelles** : en `caseless` **`deu_latf` gagne** (CER 0,075) ; `easyocr` excellent en **français** mais nul en Fraktur (0,21 agrégat). **Significativité Friedman** (n=30) : à 4 Tesseract text:cer p=0,072 (non sig.) → **avec EasyOCR p≈0 = SIGNIFICATIF** (l'écart d'architecture crée la différence détectable). Effet **langue×moteur** visible au par-document. Réalise le « mini-corpus de référence » de `CLAUDE.md §10`. | Moteurs atteignables sous la politique réseau : **Tesseract** (apt+GitHub) **+ EasyOCR** (modèles GitHub) ✓ ; **Pero/Kraken/docTR** = paquets OK mais **poids 403** (Drive/Zenodo/HuggingFace). Images/ALTO non committés (poids) → *live* = test `live` ; strate langue par-doc = enveloppe T7 ; **`by-engine` désormais justifié** (section à construire) ; `synthesis` différé |
| D-021 | 2026-06-02 | S4.b.3 by-engine | Construire `by-engine` sans **dupliquer l'overview** ni **copier les 14 métriques spéculatives** du design ? | **EngineSection** (couche 7) : une table **triée par CER** (verdict « qui gagne ») + colonne **dispersion** = `cer` min·médiane·max **par-document** (la fiabilité que l'agrégat masque). Métriques **réelles** seulement ; enregistrée overview→**by-engine**→by-document→crosses. | La **vérif-plan** a attrapé le risque : l'overview montre déjà moteurs×métriques par vue → un `by-engine` descriptif serait **redondant** (§5.3) ; et la réf design est **en avance sur la donnée** (philologie/calibration/économie non calculées). Le **tri + la dispersion** sont l'info **neuve** (ni overview ni par-document ne la donnent) ; un classement chiffré ≠ prose (narratif reste supprimé). | reste S4.b : **crosses** (à styliser au design) + **synthesis** (différé T7) → ensuite repointer « Prochaine → S5 » |
| D-022 | 2026-06-02 | S4.b.4 + clôture S4 | Styliser `crosses` au design et **clore S4** (vues rapport) ? | **CrossEngineSection** restylée `table.data` : clé `text:cer:significance_p` **éclatée** en colonnes **Vue/Métrique** + **verdict** factuel (« significatif » si p<0,05, en fern ; sinon « non sig. » ; `None`→« — »). **S4 clos** : overview/by-engine/by-document/crosses au design ; **synthesis différé T7**. | Le verdict est une **fonction auditable** de la p-value (pas de la prose, narratif §6 respecté) ; la clé brute était cryptique → Vue/Métrique + verdict = lecture directe. Clôt S4.b → **« Prochaine » repointée S4b→S5** (`CLAUDE §0` + roll-up + `NEXT_SESSION`), résolvant la **dette de pointeur** signalée depuis D-020. | reste **synthesis** (T7, avec ses métriques riches) ; `crosses` pourra gagner les comparaisons **par paires** quand le besoin existera |
| D-023 | 2026-06-02 | S5 (vérif) + clôture axe Space | « Enchaîner S5 » : que reste-t-il du durcissement public ? | **Vérif-plan** : S5 est **déjà implémenté + testé** (le « partiel » du roll-up était périmé) — mode public **enforced à l'API** (`runs.py:160`, cloud→403, source unique `CLOUD_KINDS`) · rate-limit durci (purge IP inactives, global) · CSRF · CSP `'self'` · upload double-cap. **Pas** de module `public_mode.py`/`uploads.py` séparé : la logique vit **avec son consommateur** (anti-Picarones, ≠ 6 modules `security_*`). Seul trou comblé : **test 413** (upload > `MAX_ZIP_BYTES`, monkeypatch). **S5 → axe Space S1–S5 complet.** | Bâtir des modules séparés aurait été **spéculatif** (§5.3) : le durcissement existe déjà, seule la **couverture de test** du plafond externe manquait. Repointe **« Prochaine » S5→T5** : l'axe Space est clos ; **S6 attend l'axe moteur** — la suite est côté **moteur** (segmentation / métriques). | **Pivot d'axe** : reste **T5** (structure/seg → débloque S6-seg) · **T6** (extensibilité) · **T7** (surfaces internes/métriques → débloque `synthesis` + S6-imp). T5 vs T7 = priorité produit. |

| D-024 | 2026-06-03 | T7 (métriques riches) | Acter les 3 premières métriques T7 livrées (PR #25-#27) + réconcilier le roll-up | **3 métriques philologie/profil d'erreur** câblées sur le benchmark **réel** BNL (Fraktur), chacune vérifiée informative *avant* câblage et figeant sa trouvaille par un test BNL micro+macro : **`cer_diplo`** (#25 — CER après repli ſ long→s appliqué symétriquement ; isole la pénalité typographique des Fraktur `frk`/`deu_latf`, ~12 % de leur erreur ; reste pur, réutilise `_edit_distance` + profil `minimal` couche 2) · **`diacritic_err`** (#26 — fraction des diacritiques de la réf mal reconnus par alignement `rapidfuzz.editops` ; révèle que les modèles germaniques butent sur ~80 % des umlauts vs ~12 % pour `fra`/`easyocr` ; **introduit `rapidfuzz` en prod** — sanctionné par la tranche qui l'introduit, fichier dédié `diacritics.py`, `text.py` reste pur D-007) · **`del_rate`/`ins_rate`** (#27 — profil d'erreur, réutilise `_align`/`_error_rate`, **aucune dép.** ; `easyocr` a le pire `ins_rate` → il **hallucine** là où CER/WER le noyaient ; `sub_rate` écarté car dérivable `wer−del−ins`, §5.3). Les sections génériques affichent les colonnes **sans rendu modifié** (dividende du design). Roll-up : couche 3 + ligne T7 → **🔨 partiel**. | Surface incrémentale stricte (§5.3 : additive, budget, élagage). **Ligatures écartées** (1 seule dans la GT BNL → non informatives, branche `t7-ligatures` au nom trompeur) au profit du profil d'erreur, après vérif des données. T7 reste ⏳ pour **importeurs cœur / longitudinal / `synthesis`** (ce dernier attendait précisément des métriques riches). | matérialise la ligne « Philologie/Taxonomie/… » de §3 (T7+) |

| D-025 | 2026-06-03 | T5 squelette segmentation | Bâtir le squelette ambulant de la tranche segmentation (pleine profondeur, largeur minimale) sans rien casser de l'enveloppe ? | **Squelette bout-en-bout, tout `precomputed`** : C1 `domain/layout.py` (`CanonicalLayout` + `LayoutPage/Region/Line/Word/Geometry/BBox/Point`, type **pivot** dimensionné plein-scope, géométrie en `domain` — F-1 option b) · C5 `adapters/layout/precomputed.py` (source LAYOUT régions-seules + recognizer **par région** clé `region_id`) · C4 `pipeline/fanout.py` (`run_region_fanout` : boucle régions → recognizer 1×/région → LAYOUT rempli, **échec partiel toléré**, ordre de lecture préservé) · C3 `metrics/layout.py` (`region_cer` (LAYOUT,LAYOUT) = CER par région micro-agrégé page ; **niveau absent → `None`**) + loader `LAYOUT` (`representations.py`). 6 tests bout-en-bout + unitaires verts. | **Le contrat `Module` ne bouge pas** (renvoie 1 artefact/type ; le fan-out boucle dans l'exécuteur) → « test conceptuel » T5 tenu : T5 n'**ajoute** que. Choix de largeur minimale conformes au guide `ANALYSE_T5_SEGMENTATION.md` : LAYOUT sérialisé **JSON** (pas de mapping ALTO au squelette — épaississement F), assemblage = **LAYOUT rempli** (pas ALTO_XML), pas de crop/PIL (precomputed), projecteur `layout→text`/`ProjectionSpec` **non ajoutés** (0 consommateur dans cette tranche, §5.3). Câblage corpus réel + informativité (§H du guide) différés à un épaississement (stratégie « squelette synthétique d'abord »). | matérialise le backlog `CanonicalLayout` (`CLAUDE.md`) + lève `MIGRATION_COUCHE_2.md` L10 (forme confirmée au build) |

| D-026 | 2026-06-03 | T5 épaississement (ALTO→layout) | Premier épaississement après le squelette : quel concept unique, avec consommateur de production ? | **`alto_to_layout`** (C2, `formats/alto/layout_map.py`) — projette `AltoDocument` parsé vers `CanonicalLayout` neutre (régions/lignes/mots/géométrie bbox+polygone/ordre de lecture ; `id` manquants synthétisés `region_<n>`, déterministe). **Consommateur de production** : `load_representation(LAYOUT)` détecte ALTO (1ᵉʳ octet `<`) vs JSON → une **GT de mise en page ALTO réelle** est chargeable et `region_cer` tourne dessus (test bout-en-bout). | Un seul concept (« le modèle neutre lit du vrai ALTO »), avec un **vrai consommateur** (≠ mapper testé seul, §5.3) : c'est ce qui débloque R-2 (GT layout réelle) vers l'informativité. Différés (chacun sa tranche) : **assembleur inverse `layout→ALTO_XML`** (le pendant ; le fan-out n'émet encore qu'un LAYOUT rempli), **mapper PAGE**, **dé-césure** `HypPart1/2` (concern projection `layout→texte`), **alignement IoU** (l'appariement reste par `id`), **segmenteur réel + crop**. | avance le pont `CanonicalLayout↔ALTO` du guide `ANALYSE_T5_SEGMENTATION.md` §G-2 |

| D-027 | 2026-06-03 | T5 assemblage (layout→ALTO) | Boucler le contrat `segmentation → reconnaissance → assemblage (LAYOUT→ALTO_XML)` (`CLAUDE.md` §3) ; **où** placer l'assembleur ? | **`layout_to_alto`** (C2, pendant inverse de `alto_to_layout` ; mots→`<String>`, sinon texte segmenté sur les blancs ; ordre de lecture appliqué ; octet-stable via `write_alto`) **+ `AltoAssembler`** Module en **couche 5** (`adapters/layout/assembler.py`) : LAYOUT rempli → ALTO_XML. Consommateur bouclé : l'ALTO assemblé est **rechargé comme layout** par `load_representation` (sniff ALTO) → `region_cer`. | **Placement dicté par les garde-fous d'archi** : `test_pipeline_imports_are_allowed` **interdit `pipeline → formats`** ; l'assembleur dépend de `write_alto` (formats) → il **ne peut pas** vivre en `pipeline`. Or `CLAUDE.md` §3 liste « constructeur d'ALTO » comme un **Module** (brique pluggable) et la couche 5 **peut** importer `formats` → l'assembleur est un **adapter Module**, placement à la fois légal et conceptuellement juste. Un seul concept (« assembler le layout en ALTO »), consommateur réel (≠ test seul). Différés : mapper PAGE, dé-césure, IoU, segmenteur réel, **wiring spec complet** (segmentation→fan-out→assemblage via une `PipelineSpec` unique). | clôt le pont `CanonicalLayout↔ALTO` du guide §G-2 ; lève le différé « assembleur » de la DoD couche 2 |

| D-028 | 2026-06-03 | T5 wiring spec déclaratif | Faire orchestrer les 3 étages de segmentation par une **`PipelineSpec` unique** (modules via factory), pas par des appels directs dans les tests — **comment** déclarer le fan-out dans une spec DAG linéaire ? | **Drapeau déclaratif `PipelineStep.fanout: bool`** (C1, + `model_validator` : `fanout` exige `LAYOUT`+`IMAGE` en entrée, `LAYOUT` en sortie). L'**exécuteur** (C4) branche sur `step.fanout` → délègue à **`execute_region_fanout`** (C4 : charge le LAYOUT régions-seules, fan-out reconnaissance, **persiste** le LAYOUT rempli en JSON, renvoie l'artefact ; provenance estampillée comme une étape normale). **3 builders** au registre (C6) : `precomputed_layout`, `precomputed_region`, `alto_assembler`. Test bout-en-bout : 1 spec → segment → fanout → assemblage → `ALTO_XML` rechargé en layout → `region_cer`. | Le `Module` Protocol **ne bouge toujours pas** (le fan-out reste de l'orchestration C4 ; le module renvoie 1 artefact/type). Drapeau `bool` (≠ `fanout_over: ArtifactType`) car le seul consommateur fan-out est « par région d'un LAYOUT » — typer plus serait spéculatif (§5.3). **Collision de nom résolue** : `PrecomputedLayoutSource.name` passe de `precomputed:layout` (kind `precomputed`, en conflit avec l'OCR texte) à **`precomputed_layout`** (kind distinct). Différés : mapper PAGE, crop réel, IoU, câblage corpus réel. | retire de l'exécuteur la mention « fan-out = tranche ultérieure » ; prouve l'orchestration par déclaration |

| D-029 | 2026-06-03 | T5 mapper PAGE | Symétrie ALTO : projeter PAGE XML vers le modèle neutre, avec consommateur réel. | **`page_to_layout`** (C2, `formats/pagexml/layout_map.py`) — conventions PAGE projetées vers `CanonicalLayout` : géométrie **polygones** (`coords` → `Geometry.polygon`, pas de bbox), **ligne sans mots** (`Line.words=()`), **ordre de lecture en arbre** aplati via `flatten()`, régions non-texte → `region_type` reprenant le label PRImA. **Consommateur** : `load_representation(LAYOUT)` distingue désormais **PAGE vs ALTO** au marqueur de racine (`PcGts`/`pagecontent` = PAGE) → GT layout PAGE réelle + `region_cer` (test bout-en-bout). | Même patron que `alto_to_layout` (D-026), un seul concept, consommateur de production. **Sniff XML affiné** : ALTO et PAGE commencent tous deux par `<` → désambiguïsation sur le marqueur de racine (head 4 Ko, insensible à la casse), déterministe. `_Counter` dupliqué (6 lignes) plutôt qu'un module partagé — extraire pour 2 appels serait sur-ingénierie. Différés : crop réel, IoU, câblage corpus réel. | lève le dernier mapper du différé DoD couche 2 ; ALTO **et** PAGE alimentent maintenant le même `CanonicalLayout` |

| D-030 | 2026-06-03 | T5 informativité corpus réel | Avant de s'appuyer sur l'axe structure : `region_cer` est-il **correct, non-trivial et non trompeur** sur de **vraies** données (réflexe §H du guide) ? | **Câblage sur 4 producteurs réels** (`tests/formats/fixtures` : Gallica ALTO v2, Tesseract ALTO v3, Transkribus PAGE, eScriptorium PAGE) via `load_representation`. **Findings réels** : (a) le modèle neutre porte la **diversité réelle** — bbox+mot (ALTO) vs polygone+ligne (PAGE), imbrication `ComposedBlock`+illustration (Gallica), **corps + marginalia** (eScriptorium) ; (b) `region_cer` = 0 sur identité réelle (applicable, pas `None`), `1/53` quand la marginalia (4 car. sur 53) est mal lue, `4/53` quand la **région est omise** (segmentation ratée pénalisée), `None` si niveau texte absent (pas un faux 1.0) ; (c) tout passe par le **vrai runner** `evaluate_run`. | Pas de fabrication : l'hypothèse dégradée est une **perturbation contrôlée d'une GT réelle**, clairement étiquetée (≠ run d'OCR réel). Le **fetch BNL live** reste hors-scope (politique réseau ; les fixtures producteurs committées **sont** la donnée réelle). Aucun code de prod ajouté — c'est une **preuve de câblage** (test). | valide l'axe structure « real-data-ready » ; consomme la stratégie « synthétique d'abord, réel au câblage » (D-025) |

| D-031 | 2026-06-03 | T5 projecteur layout→texte + benchmark BNL réel | Noter un pipeline de **segmentation** (sortie LAYOUT/ALTO) avec les métriques **texte** mûres, contre une référence — et le prouver sur du **vrai** OCR compétitif. | **`evaluation/projectors.py`** (`layout_to_text` : ordre de lecture, sous-régions à plat ; first-party, lookup builtin) **+ exécution `ProjectionSpec`** dans `runner._context_for` : la projection ponte le type **natif** d'un artefact → le type **consommé** par la métrique, **symétriquement** réf/hyp (lève la réserve §9 `ProjectionSpec`). **Donnée réelle** : extrait ALTO ABBYY BnL « D'Wäschfra » 1868 (CC0) committé (8,7 Ko). **Vrai benchmark mesuré** (Tesseract 5.3.4 `deu` installé, sur les TIFF réels) : **CER ABBYY-vs-Tesseract = 0.452 (manchette p1) / 0.157 (corps p2)** — non-trivial et **discriminant** (Tesseract s'effondre sur la mise en page ornementale, tient sur le corps). Test `live` opt-in (vrai binaire + `XEROCR_BNL_IMAGE`) qui rejoue la chaîne. | **Rétro-compat préservée** : `RAW_TEXT`/`CORRECTED_TEXT` restent inter-changeables (même `str`) → les post-corrections LLM (T3) ne passent **pas** par un projecteur. **Honnêteté** : ABBYY n'est pas une GT or mais l'OCR de production BnL → c'est « Tesseract vs moteur de référence », étiqueté tel quel. **Repo lean** : TIFF 11 Mo **non** committés ; extrait ALTO re-sérialisé + test `live` env-gated. Différés : crop réel, IoU. | lève la réserve §9 `ProjectionSpec` ; transforme l'axe structure en **verdict réel**, pas seulement fidélité (cf. D-030) |

| D-032 | 2026-06-03 | T5 métrique de détection de région | Mesurer la qualité de la **segmentation géométrique** (pas le texte) sur le `CanonicalLayout`, validé sur du réel. | **`region_detection`** (C3, `(LAYOUT,LAYOUT)`→F1) : régions **atomiques** (feuilles) appariées par **IoU de boîte ≥ 0.5**, **appariement glouton déterministe**, P/R/F1. **Coordonnées relatives à la page** → neutralise les unités (ALTO `mm10` vs Tesseract pixels). **Findings réels** (probe + journalisé) : ABBYY 25 régions-texte vs Tesseract 37 → **F1 = 0.35 @ IoU 0.5** (0.55 @ 0.3) : les deux producteurs s'accordent bien **moins** sur la géométrie des régions que sur le texte (CER 0.157) — Tesseract sur-segmente. Test déterministe sur géométrie réelle (extrait ABBYY, self-match F1=1.0). | **Pas de `shapely`** : IoU de **boîte** (boîte dérivée du polygone pour PAGE) couvre ALTO+PAGE → **pas de modif de whitelist `evaluation`** ; l'IoU **polygonal exact** (shapely) est différé (§5.3, aucun consommateur ne l'exige). Granularité = **feuilles** (blocs atomiques) pour éviter le double-comptage conteneur/enfant. `region_cer` reste apparié par `id` (apparier-IoU = épaississement optionnel). | complète l'axe structure (texte **et** géométrie mesurés sur le même modèle neutre) |

| D-033 | 2026-06-03 | T5 pipeline hybride réel (crop + OCR par bloc) | Rendre **réel** le cas d'usage central : « module de segmentation → OCR des **blocs** issus de la segmentation » (le `precomputed` ne cropait pas). | **`RegionCropper`** (type C4 injecté) + **`crop_region`** (C5, `adapters/layout/crop.py`, PIL) : le fan-out passe au recognizer la **sous-image découpée** du bloc. **Séparation de couches** : la couche 4 calcule la **boîte relative** (arithmétique, neutralise ALTO `mm10` vs pixels), la couche 5 fait le crop PIL (interdit à `pipeline`). Câblé jusqu'à `PipelineExecutor.execute_document(cropper=…)`. **Prouvé sur du réel** (test `live`) : régions ABBYY → crop du vrai TIFF BNL → **Tesseract par bloc** → `region_cer` **0.157**. | C'est la **convergence T5+T6** : la *mécanique* (crop + fan-out + OCR par bloc) est T5 (faite) ; brancher un **YOLO out-of-tree** sans forker = **T6** (entry-points) — le YOLO sera un `Module IMAGE→LAYOUT` dans le même emplacement. `precomputed` inchangé (cropper `None` = image entière + `region_id`). Whitelist archi `adapters` étendue à `PIL` (tranche qui l'introduit). Régions sans géométrie : non découpables → ignorées (échec partiel). | matérialise le cas d'usage central ; ne laisse comme reste que le *plug* tiers (T6) |

| D-034 | 2026-06-03 | T6 découverte de modules tiers | Ouvrir le **seul** point d'extension tiers (CLAUDE.md §3) : brancher un module de pipeline (segmenteur type YOLO) **sans forker**. | **`app/modules/discovery.py`** : `discover_plugins(registry, *, enabled, entry_points_loader=…)` scanne le groupe entry-points **`xerocr.modules`**, charge chaque builder et l'enregistre **comme le socle** (même `ModuleRegistry`, même `Module` Protocol). **Fail-closed en mode public** (`enabled=not is_public`) — pas de code tiers in-process sur serveur exposé. Plugin **défectueux / non-builder** : journalisé + ignoré (pas fatal). Câblé CLI (`enabled=True`) + web (public-aware). Plugin de **référence** : segmenteur tiers `IMAGE→LAYOUT` (`tests/fixtures/sample_segmenter_plugin.py`). **Version tierce → `RunManifest.module_versions`** : déjà gratuit (l'orchestrateur capte la version de tout module construit). | **Périmètre STRICT tenu** (§3) : seules les **briques de pipeline** sont pluggables — métriques/projecteurs/importeurs restent first-party (pas de prise ouverte). Pas de nouvelle dép : `importlib.metadata` (stdlib). `register_builder` couvre déjà le « module local » ; l'entry-point est l'ajout. `except Exception` **avec log** (résilience tierce) — autorisé (≠ `: pass`). Loader injectable → test déterministe sans pip-install ; le vrai chemin `importlib.metadata` est couvert (`test_default_loader_runs_clean`). reste : 2ᵉ impl réelle pip-installée. | matérialise l'ambition « extensibilité tierce » (CLAUDE.md §3) ; avec T5, **boucle le cas d'usage hybride** (segmenteur tiers → fan-out → OCR par bloc) |

| D-035 | 2026-06-03 | T6 preuve d'extensibilité réelle | La découverte n'était prouvée que par loader **injecté** (revue : « 327 LOC, le scan réel n'est pas testé »). Prouver le **vrai** chemin « brancher sans forker ». | **Deux preuves ajoutées** : (1) un vrai `importlib.metadata.EntryPoint` dont `.load()` fait la **résolution dotted-path réelle** (≠ fake `.load()`) ; (2) un **paquet pip-installable** (`tests/fixtures/sample_plugin_pkg/`, `pyproject` + entry-point `xerocr.modules`) réellement **`pip install`-é** puis **auto-découvert** par le scan `importlib.metadata` natif — test `slow`, **isolé** (`--target` temporaire + process enfant → zéro pollution de la session, `test_default_loader_runs_clean` reste valide), **skip-on-failure** (offline → ne casse pas la CI). | Build **avec** isolation (le setuptools Debian patché casse `--no-build-isolation` — `install_layout`). La petite taille de T6 = surtout le **dividende d'enveloppe** (registre/factory/version captés dès T2–T4) ; mais la revue a justement pointé un **vrai trou de preuve**, comblé ici. T6 désormais prouvé de bout en bout. | clôt le doute « extensibilité non prouvée » ; le scan réel d'un dist installé est couvert |
| D-036 | 2026-06-03 | Adapter Mistral + clarif. cloud-sur-Space | Pouvoir utiliser **Mistral** (et clarifier : « pas possible sur le Space ? »). | **`adapters/llm/mistral.py`** (`MistralAdapter`, `RAW_TEXT→CORRECTED_TEXT`, SDK `mistralai` en extra paresseux, calqué sur `openai.py`) + builder `mistral` au registre + statut moteur (`_mistral_status`) ; ajouté à `CLOUD_KINDS`. **Clarification** : le **mode public est OPT-IN** (`XEROCR_PUBLIC_MODE`, défaut `False`) → un Space déployé normalement **autorise** le cloud ; les clés sont lues depuis l'env (secret de Space). Le modèle visé = « clé en secret → tester → retirer → Space duplicable, chacun met SA clé » — **déjà supporté**, il ne manquait que l'adapter. | Adapter = **surface incrémentale** (« ajouter un moteur = une tranche », `CLAUDE.md §8` minimal). `except Exception`+log toléré (le SDK lève des erreurs variées ; `BLE` non sélectionné, `B904` satisfait par `from exc`). **BYO-key écarté** (inutile pour ce modèle). Numéroté **D-036** (D-034/D-035 = T6, mergés juste avant ; numérotation anti-collision sur branches empilées). | débloque le benchmark LLM cloud (local + Space non durci) |

| D-037 | 2026-06-03 | T7 — section `synthesis` (verdict factuel) | Fermer la dernière vue du rapport (S4) : un **verdict chiffré** en tête, sans ressusciter le narratif (supprimé §6) ni générer de prose (anti-hallucination §12). | **`reports/sections/synthesis.py`** (`SynthesisSection`, contrat `Section` unique) : par vue, **meilleur pipeline selon le CER** (plus bas = meilleur), **Δ CER** au suivant, **significativité** reprise de `RunResult.cross_engine` (clé `view:cer:…`, sig si p < 0,05). Montée **en tête** du `default_report_renderer`. **Gatée `requires=("cer",)`** → sautée par le no-orphan si aucune vue ne porte de CER. | **Déterministe & auditable** : noms *verbatim*, nombres = minimum / soustraction / p-value (zéro fabrication ; le `—` apparaît quand une donnée manque, ex. pas de p-value inter-moteurs dans la démo). **Métrique phare = `cer` en dur** (constante `_PRIMARY_METRIC`, commentée) : choix assumé (CER = métrique titre du produit), pas spéculatif — un seul consommateur (le renderer). Tri `(cer, pipeline)` → stable aux égalités. 107 LOC (< budget). | ferme **S4** (toutes les vues au design) ; reste T7 = importeurs cœur · longitudinal |

| D-038 | 2026-06-03 | T7 — 1ᵉʳ importeur de corpus (IIIF) | Ouvrir l'axe **importeurs cœur** (débloque S6-imp) par le plus simple : IIIF (manifeste → images). Sans rejouer les dettes Picarones (sortie non uniforme, bugs masqués par les mocks, `install_opener` global, données démo en dur). | **`adapters/corpus/_http.py`** : transport `httpx` **anti-SSRF** (http(s) seul, rejet userinfo + IP non publiques résolues, redirections **re-validées** à chaque saut, taille plafonnée au fil de l'eau, **aucun état global**). **`adapters/corpus/iiif.py`** : parser manifeste **v2/v3** (auto-détecté) → `IIIFImage` (URL+label), **pur** ; `IIIFImporter` = fetch+parse, **sans disque**. **`app/corpus_import.py`** : `import_iiif_corpus` matérialise (download → `CorpusSpec`), `importer`/`download` **injectables**. Sortie **unique `CorpusSpec`** (D-F clos), `validated_path` sur les fichiers (R-6). | **R-5 tenu** : adapter = *localiser* (transport+schéma), `app` = *matérialiser* (disque) → sortie `CorpusSpec` (pas de dict-manifeste). **F9/R-7 tenu sans mock du transport** : test **loopback réel** (`http.server` sert un vrai manifeste v3 + PNG → vrai chemin `httpx` fetch→parse→download→`CorpusSpec`, octet-exact), déterministe/CI ; **+** test `live` distant (opt-in) — *non vérifiable dans ce sandbox : allowlist réseau (« Host not in allowlist ») bloque tout hôte IIIF ; signalé, non masqué*. **Images seules = choix assumé** (manifeste IIIF = pages, pas de GT → `DocumentRef.ground_truths=()`, OCR-able non scoré ; confirmé avec l'utilisateur). **Garde-fou conso** : consommateur produit = **S6** (importeur-UI, prochain, désormais débloqué) ; exercice CI = test transport réel (≠ test-only mocké). Une seule pile HTTP (`httpx`, déjà cœur). | ouvre `corpus/` (couche 5) ; débloque **S6-imp** ; reste importeurs Gallica/eScriptorium/HTR-United/HF (1/tranche) |

| D-039 | 2026-06-03 | T7 — importeur eScriptorium (corpus **scorable** + R-7) | 2ᵉ importeur cœur : passer d'« images seules » (IIIF) à un corpus **avec vérité-terrain** (boucle import→benchmark→CER complète). Fermer le bug latent **R-7/D-I** (`Corpus(source=…)` → `TypeError`, masqué par les mocks) en le rejouant sur du transport réel. | **`adapters/corpus/escriptorium.py`** : `EScriptoriumImporter` (auth `Authorization: Token …`, pagination `next`, fetch parts + transcriptions) + `extract_gt_text`/`_image_uri` **purs** ; `_http.fetch_json` gagne un param **`headers`** (auth d'API ; jamais journalisé). **`app/corpus_import.py`** : `import_escriptorium_corpus` télécharge l'image **et** écrit le texte de la couche → `GroundTruthRef(RAW_TEXT, .gt.txt)` → **`CorpusSpec` scorable**. Page sans texte = image-seule ; sans image = ignorée (avertie). | **R-7 structurellement fermé** : XerOCR n'a pas de `Corpus(source=)` — `source` est une clé de `metadata: dict[str,str]`, pas un kwarg de constructeur → le `TypeError` historique est **impossible**, et c'est **prouvé sur la vraie donnée** par `test_escriptorium_local_server.py` (un `http.server` sert une API eScriptorium paginée + images → vrai chemin `httpx` : **auth header vérifié**, **pagination `next` traversée**, GT extraite des deux formes lignes/`content`, `CorpusSpec` construit sans erreur). GT typée **`RAW_TEXT`** (convention `corpus_upload`). **Pas de test `live`** : eScriptorium = instance privée + token (aucune cible publique) → le loopback réel **est** la couverture transport. **Limite assumée** (incrémentale) : download d'image **sans** header d'auth (média souvent ouvert ; média gardé = à brancher au besoin). | 2ᵉ importeur cœur ; **R-7 clos** ; reste Gallica/HTR-United/HF |

| D-040 | 2026-06-03 | T7 — importeur Gallica (réutilise IIIF + OCR étiqueté) + R-7 #2 | 3ᵉ importeur cœur. Décision méthodo (confirmée utilisateur) : **images + OCR Gallica comme *référence étiquetée***, pas comme GT manuelle. Fermer la 2ᵉ moitié de R-7 (`selected_indices[i]+1`, décalage page→OCR masqué par les mocks). | **`adapters/corpus/gallica.py`** : `normalize_ark` (`ark:/12148/x`→`12148/x`, pur), `GallicaImporter` (`manifest_url` + `fetch_ocr_text` via `texteBrut`, filtrage des réponses HTML des vues sans OCR) ; `base_url` surchargeable (test transport). **`_http.fetch_text`** (corps texte, pas JSON). **`app/import_gallica_corpus`** : images via **`IIIFImporter` réutilisé** (manifeste IIIF Gallica) + OCR écrit en `*.gallica_ocr.txt` → `GroundTruthRef(RAW_TEXT)`, `gt_source=gallica_ocr` en `metadata`. Helper `_download_image` factorisé (IIIF/eScriptorium/Gallica). | **R-7 #2 clos par conception** : le numéro de vue Gallica **est** la position (1-based) dans le manifeste → `vue i ↔ f{i}.texteBrut`, **aucune indirection `selected_indices`** (origine du décalage). Prouvé non-mocké par `test_gallica_local_server.py` (loopback sert manifeste v2 + images + `texteBrut` : f1=texte→GT, f2=HTML→filtré image-seule ; mapping + filtre vérifiés sur vrai `httpx`). **OCR ≠ GT** : étiqueté `gt_source=gallica_ocr` (corpus-level) + nom de fichier explicite. **Pas de `live`** : loopback réel = couverture transport (Gallica public = pas d'auth, mais l'allowlist sandbox bloque l'hôte réel ; le loopback suffit et tourne en CI). **Surface non reprise** : SRU/search, OAI metadata, `get_metadata` (0 conso). | 3ᵉ importeur cœur ; **R-7 entièrement clos** ; reste HTR-United/HF · longitudinal |

| D-041 | 2026-06-03 | T7 — découverte HTR-United + HuggingFace (importeurs cœur complets) | Porter les 2 dernières sources comme **découverte** (lister/chercher des corpus candidats), pas matérialisation : HTR-United pointe vers des dépôts ALTO/PAGE (clone = ultérieur), HF nécessite la lib lourde `datasets` (extra ultérieur). Feed direct de **S6** (UI sélection de corpus). | **`adapters/corpus/htr_united.py`** : `parse_catalogue` (YAML, pur, tolérant au schéma), `HTRUnitedCatalogue` (recherche + `from_demo`/`is_demo`), `fetch_catalogue` (télécharge l'index, **repli démo silencieux** si réseau/`YAMLError`). `yaml` ajouté à `ADAPTERS_ALLOWED_EXT`. **`adapters/corpus/huggingface.py`** : `search_reference` (socle intégré, pur) + `HuggingFaceCatalogue.search` (API Hub **best-effort** : échec → socle référence seul, jamais bloquant ; `source` ∈ {reference, api}, dédup). `_http.fetch_text`/`fetch_json` réutilisés. | **Discovery ≠ matérialisation** : sortie = entrées de catalogue (pour browse UI), pas un `CorpusSpec` (pas de download). **Repli gracieux testé** : HTR-United → `is_demo` ; HF → référence seule (les deux par `fetch_*` qui lève `HttpFetchError`/`YAMLError`, capté). **Transport prouvé non-mocké** : `test_htr_united_local_server.py` (YAML servi → `source=remote`) + `test_huggingface_local_server.py` (API JSON servie → `source=api`). **Socle de référence/démo gardé petit et inline** (≠ dump Picarones) — assumé, pas un `data/` packagé (incrémental si ça grossit). **Surface non reprise** : `_fallback_log`/détecteur narratif, OAI metadata, SRU search, `get_metadata`. | **importeurs cœur COMPLETS** (IIIF · eScriptorium · Gallica · HTR-United · HF) ; reste **longitudinal** pour clore T7 → débloque **S6** |

| D-042 | 2026-06-03 | T7 — historique longitudinal SQLite | Dernier gros bloc T7 : suivre les runs dans le temps (tendance + **régressions**), brique de la vue Historique S6. Sans rejouer la dette Picarones (store SQLite éparpillé, détecteurs narratifs `regression_in_history` — narratif supprimé §6). | **`adapters/storage/history_store.py`** : `HistoryStore` SQLite. `HistoryRecord`/`Regression` = dataclasses frozen. `add` (idempotent, PK `(run_id,pipeline,view,metric)`), `history` (chronologique), `regressions(view, metric, threshold, higher_is_better)` (compare les 2 derniers runs par pipeline, **direction-aware** : CER/WER plus-bas-meilleur par défaut). 1 connexion SQLite/opération (thread-safe, pas de `:memory:`). **`app/history.py`** : `records_from_run` (aplatit `RunResult`→lignes, `None` écartés) + `record_run`. | **Séparation des couches tenue** : le **store ne connaît que des lignes primitives** (n'importe **pas** `RunResult`/couche 3) → reste persistance pure, réutilisable ; la connaissance de `RunResult` vit en `app` (couche 6, qui parle déjà évaluation). **Régression direction-aware** (pas de « plus bas = mieux » câblé en dur) : `higher_is_better` paramétrable (défaut False = CER/WER). **Prouvé sur vrai SQLite** (`tests/adapters/storage/test_history_store.py`) : round-trip chronologique, **idempotence** (re-record même PK), **persistance inter-instances** (réouverture du fichier), régression détectée/non (amélioration), **seuil**, **inversion de sens**. **Garde-fou conso** : producteur = l'orchestrateur de run (câblage auto **différé** — attend la config du chemin DB, axe S) ; consommateur requête = S6 (vue Historique) / CLI `history` ; exercice CI = test round-trip réel. **Dette non reprise** : `_fallback_log`, détecteurs narratifs, `evaluation/metrics/{history,longitudinal}.py` (regression as-metric). | clôt le bloc **longitudinal** de T7 ; reste **philologie** (métriques) ; débloque la vue Historique **S6** |

| D-043 | 2026-06-03 | T7 — métrique philologique `mufi_err` (clôt le scope T7 listé) | Dernier item T7 : la **philologie** au-delà des diacritiques. Mesurer ce qu'un moteur rate spécifiquement sur les **glyphes médiévaux** (lettres/abréviations MUFI), invisible dans un CER global. | **`evaluation/metrics/philology.py`** : `mufi_err` (`@document_metric`, `RAW_TEXT×RAW_TEXT`). Prédicat `_is_mufi` : zone d'usage privé (PUA, où vit MUFI) + Latin Extended-D (abréviations latines) + Combining Diac. Supplement + ligatures latines + set explicite (`ſ þ ð ƿ æ œ ø ƀ ŧ đ ħ ȝ`…). Parmi les caractères MUFI de la **réf**, fraction mal reconnue (replace/delete via alignement `rapidfuzz`), `None` si aucun (N/A exclu de l'agrégat). Enregistré au socle (`registry.py`). | **Recopie le noyau sain** (jeu de codepoints raisonné de la source, médiéval européen) en **élaguant** : narrow des Presentation Forms à FB00–FB06 (ligatures latines, exclut l'hébreu) ; **pas d'overlap** avec `diacritic_err` (lui = catégorie combinante `Mn` ; ici = lettres/PUA/abréviations). **Même contrat, additif** : 0 changement d'enveloppe `RunResult`, N/A sur corpus modernes (n'pollue pas l'agrégat — prouvé : 294 tests verts hors régression). **Déterministe** (`rapidfuzz` C, whitelist `evaluation/`). — *preuves : `tests/evaluation/test_metrics_philology.py` (PUA/Latin-D/ligature/long-s, N/A, substitution, partiel), `…/test_registry.py` (socle = 10 métriques doc)*. | **scope T7 listé COMPLET** (importeurs · longitudinal · philologie · métriques) ; familles riches (NER/taxonomy/calibration) **additives** ; débloque **S6** |

| D-044 | 2026-06-03 | S6.a — importeur IIIF côté UI (1ʳᵉ surface S6) | T7 a livré les importeurs **en bibliothèque** ; S6 les expose en UI. Première surface : brancher l'import IIIF au web pour qu'un corpus distant devienne **cible d'un run** sans passer par un ZIP. | **`interfaces/web/routers/corpus.py`** : `POST /api/corpus/import/iiif` (DTO `IIIFImportRequest` strict : `manifest_url` + `name?` + `limit?` 1–500, **CSRF**) → `import_iiif_corpus` via `CorpusStore.materialize`. **`app/corpus_upload.py`** : `CorpusStore.materialize(builder)` — registre **agnostique de la source** (alloue id+dossier, appelle un builder `(dest)→CorpusSpec`). `create_app` propage `public_mode` au routeur corpus. | **Sécurité = décision centrale** : l'import **fetch une URL côté serveur** → **gate mode public 403** (même posture que les moteurs cloud `CLOUD_KINDS`), en plus de l'anti-SSRF d'`_http` (défense en profondeur : on n'ouvre pas la prise sur un Space exposé). **Réutilisation propre** : `materialize` générique (pas de `import_iiif` spécialisé dans le store) → eScriptorium/Gallica brancheront le même seam ; corpus importé **immédiatement sélectionnable** (intègre le store existant, pas un silo). **CSRF** sur l'écriture. Erreurs mappées (`SsrfError`/`CorpusImportError`→422, corps invalide→422 Pydantic). — *preuves : `tests/interfaces/web/test_corpus_import_api.py` (5 cas)*. **Garde-fou conso** : l'endpoint **est** le consommateur de `import_iiif_corpus` (plus seulement les tests) → boucle fermée. **Reste S6.b** : form HTML/JS au design (le `<select>` corpus de `/benchmark` consommera l'id) — non testable en CI (pas de navigateur). | ouvre **S6** (1ʳᵉ surface UI importeur) ; reste form UI + eScriptorium/Gallica/découverte + segmentation-UI + Historique |

| D-045 | 2026-06-03 | S6.a — eScriptorium + Gallica côté UI (même seam) | Compléter l'importeur-UI : exposer les 2 autres importeurs matérialisants en réutilisant le seam de D-044, sans dupliquer la logique gate/erreurs. | **`interfaces/web/routers/corpus.py`** : `POST /api/corpus/import/escriptorium` (DTO : `base_url`+`token`+`doc_pk`+`name?`+`layer`+`limit?`) et `…/gallica` (DTO : `ark`+`name?`+`limit?`+`include_ocr`). Helper local **`_materialize(builder)`** factorise gate mode public + `store.materialize` + map d'erreurs ; `import_iiif` refactoré pour l'utiliser (les 3 endpoints = coquilles). `_IMPORT_ERRORS = (CorpusImportError, CorpusHttpError, GallicaArkError)`. | **Un seul chemin** (DRY) : gate/erreurs/résumé en un point, 3 builders triviaux → ajouter un importeur = 1 DTO + 1 endpoint de 3 lignes. **Même posture sécurité** : fetch serveur → **403 mode public** sur les 3 ; token eScriptorium dans le corps (Space privé), **non journalisé** (l'`_http` ne logue que l'URL). **Mapping d'erreurs étendu** : `GallicaArkError` (ARK malformé) → 422. **Test du vrai chemin sans réseau** : `test_gallica_bad_ark_is_422` envoie un ARK invalide → `normalize_ark` lève **synchroniquement** (avant tout fetch) → 422 — exercé sans mock. Les cas réseau restent mockés (transport déjà prouvé en loopback couche 5, D-039/D-040). — *preuves : `tests/interfaces/web/test_corpus_import_api.py` (12 cas : 4 IIIF + 4 eScriptorium + 3 Gallica + helpers)*. | **S6.a complet** (3 importeurs matérialisants en UI) ; reste form UI + découverte (HTR-United/HF) + segmentation-UI + Historique |

| D-046 | 2026-06-03 | S6 — Historique : câblage recording + API lecture (ferme le différé D-042) | Le longitudinal (D-042) était **inerte** (pas de producteur câblé, garde-fou conso ouvert). Le brancher de bout en bout : chaque run terminé alimente l'historique, exposé en lecture pour la future vue Historique. | **`app/jobs.py`** : `JobRunner` gagne `history_store` optionnel ; `_record_safe(result)` appelle `record_run` après `dump`/`publish` (**best-effort** : `except Exception`→log, ne fait jamais échouer le run — même posture que `_publish_safe`). **`interfaces/web/routers/history.py`** : `build_history_router` → `GET /api/history/series` + `…/regressions` (lecture seule, `dataclasses.asdict`). **`app.py`** : `HistoryStore(catalog_dir/"history.db")` partagé runner+routeur. | **Garde-fou conso fermé** : le `JobRunner` est désormais le **producteur réel** (plus seulement les tests). **Best-effort assumé** : l'historique est secondaire au run — un SQLite en erreur ne perd pas le `RunResult` (déjà écrit + publié). **Lecture seule** : `GET` only → pas de CSRF, pas de réseau → pas de gate public. **Test de bout en bout déterministe** : run démo `precomputed` réel via `JobRunner` → `(pipeline,vue,métrique)` **découverte** depuis le `RunResult` écrit (robuste aux détails démo) → présente dans l'historique ; + historique **optionnel** (`None`→run aboutit). | **D-042 différé levé** ; reste **page** Historique au design (rendu serveur, testable) + autres surfaces S6 |

| D-047 | 2026-06-03 | S6 — page « Historique » (rendu serveur) | Rendre le longitudinal **visible** : une vue qui montre les régressions et le journal des mesures, dans le chrome du design, sans JS. | **`interfaces/web/templates/history.html`** (étend `base.html`) : hero (nb mesures) + section **Régressions** (table ou « aucune ») + section **Journal** (table récent→ancien ou « vide »). **`home.py`** : route `/history` (nav `history` promue dans `_LIVE_VIEWS`) — lit `history_store.all_records()`, dérive les `(vue,métrique)` distincts, agrège les `regressions` par paire ; gagne le param `history_store`. **`adapters/storage/history_store.py`** : `all_records()` (énumération `ORDER BY completed_at DESC`). **i18n** FR/EN `history_*`. | **Page = consommateur** des endpoints/store de D-046 (boucle UI fermée). **100 % rendu serveur** (aucun JS → entièrement testable en CI, contrairement aux forms d'import/segmentation). **Lecture du store, pas de ré-agrégation** `evaluation/` (CLAUDE §8.3) : les régressions viennent de `HistoryStore.regressions`, le journal de `all_records`. **Test data-driven sans run threadé** : `build_home_router` monté avec un `HistoryStore` **pré-amorcé** (Jinja2 réel) → contrôle total du contenu rendu ; + smoke `create_app` (état vide, lien nav). | **page Historique livrée** ; reste S6 : form UI imports + découverte UI + segmentation-UI |

| D-048 | 2026-06-03 | S6 — page « Bibliothèque » (découverte HTR-United + HF) | Exposer les catalogues de découverte (D-041) en UI : parcourir/chercher des corpus candidats. | **`interfaces/web/templates/library.html`** (étend `base.html`) : hero + formulaire `GET ?q=` + section HTR-United (badge démo si `is_demo`) + section HuggingFace (id, langues, téléch., lien). **`home.py`** : route `/library` (nav `library` promue *live*) → `fetch_catalogue()` (réseau + repli démo) + `HuggingFaceCatalogue().search(q)` (réf + API best-effort) ; importe les deux fns de découverte. **i18n** FR/EN `library_*`. | **Pas de JSON endpoints** (la page rend côté serveur → des endpoints sans consommateur JS seraient spéculatifs, §5.3) — la **page est le consommateur** des catalogues de D-041. **100 % rendu serveur** → testable en CI (≠ form d'import JS). **Best-effort, jamais bloquant** : HTR-United hors-ligne → repli démo (badge `is_demo`), HF API down → socle référence. **Liens externes** `rel="noopener noreferrer"` + `target=_blank`. **Test sans réseau** : `fetch_catalogue`/`HuggingFaceCatalogue` **monkeypatchés** dans le module `home` → contrôle total, 0 appel réseau ; requête vérifiée transmise à la recherche. *Limite assumée* : fetch par chargement de page (cache = incrémental). | **page Bibliothèque livrée** ; reste S6 : form UI d'import (relie `/library`/`/benchmark` aux endpoints `import/*`) + segmentation-UI |

| D-049 | 2026-06-04 | Correctif CI rouge (PR #31) — 3 causes | La CI était **rouge** (tous les jobs `test` + `docker`) alors que mes *sweeps locaux par sous-ensemble* étaient verts : **je n'avais jamais lancé la suite complète**. Diagnostic via les logs d'actions. | **(1) `/data` PermissionError** (3× `test_app`) : `HistoryStore.__init__` faisait un `mkdir` eager ; `create_app()` (chemin défaut `/data`, non writable hors Space) plantait à la construction → **init paresseuse** (`_connect()` crée dossier+schéma à la 1ʳᵉ opération). **(2) `test_iiif_live`** tournait en CI (réseau dispo, ≠ sandbox) et assertait `https://` alors que le Cookbook sert `http://` → **skip opt-in** (`XEROCR_LIVE_IIIF`, convention BNL) + assertion `http(s)`. **(3) docker** : la vitrine importe les importeurs de corpus → `httpx`, **absent de `[serve]`** → `serve` levait « serveur non installé » → **`httpx` ajouté à `[serve]` + `deploy/requirements.txt`** (audit : `httpx` = seul ext non-cœur du graphe d'import web ; `yaml` déjà cœur). | **Leçon de process** : lancer `pytest -q` **complet** avant push (les sous-ensembles masquent les régressions transverses : factory `create_app`, deps d'image, tests réseau). **Garde-fou renforcé** : `test_app` (appelle `create_app()` sans args) verrouille désormais l'absence d'I/O eager à la construction ; le job `docker` verrouille la complétude de `[serve]`. **Init paresseuse** : aucun effet de bord filesystem à la construction du store (cohérent avec « pas d'effet de bord à l'import/instanciation »). | CI repassée verte (à confirmer) ; reprise S6 ensuite |

| D-050 | 2026-06-04 | Audit impitoyable de la session — correctifs | Audit critique (sous-agent + revue) du code de la session (importeurs · historique · `mufi_err` · UI S6). Objectif : distinguer le solide du bricolage. | **Sécurité** — (1) **fuite de jeton sur redirection cross-hôte** (`_http._send_validated`) : les en-têtes (`Authorization`) suivaient une 30x vers un autre hôte → **strip si l'hôte change** (+ test 2 serveurs loopback). (2) **`next` eScriptorium cross-hôte** envoyait le jeton à un tiers → `_paginate` **refuse un `next` hors hôte**. (3) **DNS-rebinding** : doc honnête — validation IP = défense best-effort (double résolution), la vraie barrière est le **gate mode public 403** ; pinning IP = futur. **Correctness** — (4) **Gallica décalage de page** : la position dans la liste d'images ≠ vue Gallica dès qu'un canvas sans image est sauté → vue lue dans l'URL (`/f{n}/`, `vue_number`), position en repli ; **test avec canvas sauté** (échouait avant). (5) **déterminisme historique** : `ORDER BY … , run_id` (bris d'égalité sur timestamps identiques). (6) `_extract_label` pouvait renvoyer non-`str` → `str()`. **Robustesse/UI** — (7) `all_records(limit=1000)` borne la page Historique. **Tests manquants comblés** : `materialize` direct (alloc/échec/unicité), `vue_number`, label v3 non-str. | **Mea culpa process** : la CI était rouge depuis D-038 (1ʳᵉ tranche) et je ne l'ai vu qu'à D-049 — je rapportais « vert » sur des **sous-ensembles**. Désormais : **suite complète obligatoire** avant push (faite ici : **664 passed, cov 95.4 %**). **Non corrigé, assumé & documenté** : OCR Gallica typé `RAW_TEXT` (étiqueté `gt_source`, pas de type domaine dédié) ; `download` bufferise en RAM (cap respecté, écriture atomique sans fichier partiel — choix) ; catalogues HF/HTR-United = découverte *display* (matérialisation = ultérieure). | CI verte (D-049) ; audit fixes verrouillés par tests ; reprise S6 ensuite |

| D-051 | 2026-06-04 | Lot A — anti-DNS-rebinding **fermé** (épinglage d'IP) | `_http` résolvait l'hôte pour valider les IP, puis `httpx` re-résolvait à la connexion → un DNS hostile pouvait montrer une IP publique au validateur et une IP interne (127.0.0.1 / 169.254.169.254) à la connexion (limite « best-effort » assumée en D-050). | **Résolution unique + épinglage.** `assert_public_url` résout **une fois**, valide **toutes** les IP, et **retourne** les IP publiques. La connexion vise directement l'IP épinglée via un `network_backend` `httpcore` custom (`_PinnedBackend.connect_tcp` substitue l'IP validée, repli multi-adresses dans l'ordre). `_send_validated` devient `_stream_validated` (contextmanager) : un client `httpx` **par saut**, ré-épinglage à chaque redirection, strip `Authorization` cross-hôte conservé (D-050), cap + redirections bornées inchangés. `httpcore` ajouté à la whitelist archi adapters + extras `dev/serve/ollama` + `deploy/requirements.txt`. | **Pas de demi-pinning** (garde-fou anti-bricolage du Lot A) : on n'override **pas** le SNI sur une URL pointant l'IP (fragile selon la version `httpx`) — l'**URL reste le vrai hôte**, donc `Host`, **SNI** et vérification du certificat TLS portent naturellement sur l'hôte réel ; seule la cible TCP est figée. Le `isinstance(pool, ConnectionPool)` fait échouer **bruyamment** si `httpx` change la forme du pool (seul point d'injection : `httpx` n'expose pas `network_backend`). Le gate public 403 reste la 1ʳᵉ barrière ; l'épinglage est désormais la défense en profondeur **étanche**. **Non-régression** : les loopback-tests neutralisent `assert_public_url` (→ `None` = pas d'épinglage, résolution httpx normale) → restent verts **sans modification**. — *preuve : `test_connection_pins_validated_ip_not_rebind` (getaddrinfo hostile public→loopback, interception de la cible TCP réelle, 1 seule résolution)*. | Lot A clos ; reste Lots B→G (cf. `REMEDIATION_PLAN.md`) |

| D-052 | 2026-06-04 | Lot B — robustesse transport (download flux disque + auth média même-hôte) | **B1** : `download` bufferisait tout le corps en RAM avant `write_bytes` (limite assumée D-050) — risque mémoire sur grosses images + écriture non atomique. **B2** : le download d'image eScriptorium ne portait **jamais** l'`Authorization` → média gardé inaccessible (limite D-039). | **B1** — `download` écrit **au fil de l'eau** dans un `.part` voisin (jamais tout en RAM), plafonne à `IMAGE_MAX_BYTES` pendant la lecture, puis `os.replace` **atomique** vers `dest` ; `.part` supprimé via `except BaseException` (statut/dépassement/I/O/annulation) → **aucun fichier partiel**, `dest` n'apparaît que complet. **B2** — `download` gagne `headers` (transmis à l'hôte d'origine, retirés cross-hôte par `_stream_validated`) ; `Downloader` (couche 6) devient un `Protocol` avec `headers` ; `import_escriptorium_corpus` ne joint le jeton qu'au média **même-hôte** que `base_url` (média CDN tiers → sans jeton, warning). | **`os.replace` atomique** sur le même dossier (même FS, `.part` voisin de `dest`) — garantie « pas de demi-fichier ». **Double barrière B2** : décision même-hôte **avant** la 1ʳᵉ requête (le strip de `_stream_validated` ne couvre que les redirections) ; un média tiers ne reçoit jamais le credential, même au premier saut. **Surface inchangée** : IIIF/Gallica passent `headers=None` (médias publics). — *preuves : `tests/adapters/corpus/test_http_download.py` (flux atomique + 0 `.part`, dépassement de cap → 0 fichier, auth transmise à l'origine), `tests/app/test_corpus_import_escriptorium.py::test_media_auth_only_same_host`*. | Lot B clos ; reste Lots C→G (cf. `REMEDIATION_PLAN.md`) |

| D-053 | 2026-06-04 | Lot C — type « OCR-référence » `REFERENCE_TEXT` (D1), matérialisé de bout en bout | L'OCR Gallica (`texteBrut`) était écrit comme GT `RAW_TEXT` → **scoré comme une vérité-terrain manuelle**, alors qu'il est lui-même un OCR : un « CER » bas ne dit que « d'accord avec Gallica », pas « correct » (faux verdict, ≠ §12 verdict factuel). | **domain** : `ArtifactType.REFERENCE_TEXT` (valeur `reference_text`). **adapters/eval** : `REFERENCE_TEXT` chargé comme texte (`representations`) + projecteur `identity_text` (str→str, étiquette « référence OCR » → espace `RAW_TEXT`). **app** : Gallica écrit `GroundTruthRef(REFERENCE_TEXT)`. **interfaces** : `_REFERENCE_VIEW` (opt-in via projection `reference_text→raw_text`) + `_views_for_corpus(corpus)` construit les vues d'après les **types de GT présents** (RAW_TEXT→vue `text` ; REFERENCE_TEXT→vue référence ; aucune→`text` non scoré) ; `_tesseract_spec` le consomme. **reports** : 0 code — le **nom de vue** porte l'avertissement, rendu tel quel. | **Anti-dormant (garde-fou §5.3/§5.5, exigé par l'utilisateur)** : le type n'est pas ajouté « au cas où » — il est **consommé de bout en bout** (Gallica l'écrit → `_views_for_corpus` l'active → vue référence le score → rapport l'affiche distinctement). La vue `text` par défaut l'**ignore** (pas de projection) → plus de faux score GT. **Pas de métrique « divergence » dupliquée** : on réutilise `cer/wer/mer`, la distinction vit au niveau **vue** (nom + `ignored_dimensions`). **Honnêteté `identity_text`** : projecteur réel et **invoqué** (REFERENCE_TEXT hors `_TEXT_LIKE` du runner → passe par la projection, pas par le court-circuit text-like) — pas un nom bidon. escriptorium garde `RAW_TEXT` (transcription = vraie GT). — *preuves : `tests/evaluation/test_reference_text.py` (vue référence score 1/11, vue défaut → None/support 0 ; identity_text rejette non-str), `tests/interfaces/web/test_views_for_corpus.py` (sélection par types de GT), `tests/app/test_corpus_import_gallica.py` (GT = REFERENCE_TEXT), `tests/domain/test_artifacts.py` (valeur + round-trip)*. | Lot C clos ; reste Lots D→G |

| D-054 | 2026-06-04 | Lot D — import HuggingFace (convention XerOCR + streaming) | Le HF n'était que **découverte** (lister des datasets) ; pas de **matérialisation** en corpus scorable. D2 : convention XerOCR seule + streaming (pas de snapshot local complet ; lib `datasets` lourde). | **doc** : `docs/corpus_huggingface.md` (colonnes `image`+`ground_truth` ; `segmentation` réservée). **adapters** : `corpus/huggingface.stream_pages` — import **paresseux** de `datasets` (extra `[huggingface]`), `streaming=True`, `Image(decode=False)` → **octets** (aucune dépendance PIL), conformité validée sur la 1ʳᵉ ligne, image > `IMAGE_MAX_BYTES` ignorée, `limit` borne le nombre de pages ; `HFPage` (type neutre adapter→app), erreurs `HuggingFaceConventionError`/`HuggingFaceUnavailableError` ; `loader` injectable. **app** : `import_hf_corpus` matérialise page-par-page → image + GT `RAW_TEXT` (vraie GT d'un dataset curé) → `CorpusSpec`. **interfaces** : `POST /api/corpus/import/huggingface` (DTO strict, CSRF, **gate public 403**), non-conforme → **422**, extra absent → **409**. `datasets` whitelisté archi adapters. | **Pas de snapshot** : streaming réel, `limit` borne, jamais le dataset entier. **Pas de PIL** : octets bruts via `decode=False` (la frontière adapter→app ne porte jamais d'objet image). **Testable sans la lib** : `loader`/`stream` injectables → CI couvre conformité (422), octets manquants, cap, `limit`, matérialisation GT, et les cas HTTP (201/CSRF/403/422/409) ; le vrai `datasets` = extra (test opt-in hors sandbox). **GT = vraie `RAW_TEXT`** (≠ `REFERENCE_TEXT` de Gallica : un dataset XerOCR curé est une vérité-terrain manuelle). — *preuves : `tests/adapters/corpus/test_huggingface_import.py`, `tests/app/test_corpus_import_hf.py`, `tests/interfaces/web/test_corpus_import_hf_api.py`*. | Lot D clos ; reste Lots E→G |

| D-055 | 2026-06-04 | Lot F — UX/scalabilité (cache TTL catalogues + import atomique) | **F1** : `/library` refetchait HTR-United **et** HF à **chaque** chargement de page (réseau systématique). **F3** : `CorpusStore.materialize` laissait un dossier **partiel** sous `base_dir` si le builder échouait à mi-parcours (réseau, source non conforme). | **F1** — `interfaces/web/_cache.py::TTLCache` (générique, thread-safe, **horloge injectable**, calcul hors verrou, échec non caché) ; `build_home_router` instancie deux caches (HTR-United keyé `"htr_united"`, HF keyé par requête `q`), TTL 300 s → `/library` ne refetch plus par chargement (`.search(q)` HTR-United reste en mémoire). **F3** — `materialize` enveloppe l'appel builder d'un `except BaseException` → `shutil.rmtree(dest, ignore_errors=True)` puis `raise` : **aucun dossier de corpus résiduel**, rien d'enregistré. | **F2 différé** (import = job de fond annulable/SSE) : **pas de consommateur** aujourd'hui (corpus petits/moyens, `limit` borne) → on ne pose pas l'infra d'avance (garde-fou §5.3). **Caches = consommateur réel** (`/library`), pas spéculatif. **Atomicité cohérente** avec le `.part`/`os.replace` du `download` (D-052) : même posture « rien à demi laissé derrière ». — *preuves : `tests/interfaces/web/test_cache.py` (TTL, expiration, échec non caché), `test_library_page.py::test_library_caches_catalogue_across_loads` (1 fetch pour 2 loads), `tests/app/test_corpus_store_materialize.py::test_materialize_failure_cleans_partial_dir`*. | Lot F clos (F2 différé) ; reste **Lot G** (E = hors sandbox) |

| D-056 | 2026-06-04 | Lot G — housekeeping (`except` de `jobs.py` vérifiés + accepts mineurs documentés) | Audit : passer les `except Exception` de `jobs.py` en revue (narrower si possible) ; documenter 4 « accepts » mineurs restés implicites. | **`jobs.py`** : les 3 catch-all sont **vérifiés intentionnels** (et déjà conformes à `no_broad_except` : ils loguent). Conclusion : **ne pas narrower** — (1) catch-all worker = un thread ne meurt jamais en silence (→ FAILED, jamais bloqué RUNNING) ; (2) `_record_safe`/`_publish_safe` = **effets secondaires** : narrower laisserait une erreur inattendue remonter au catch-all worker et marquer **FAILED un run réussi** (RunResult déjà écrit). Commentaires de rationale renforcés + **tests de non-régression** (publish/historique en échec → run **DONE**, résultat écrit). **Accepts documentés** (commentaire `Accept #n` au site) : #9 HF `results[:limit]` (réf prime sur l'API) ; #14 part eScriptorium sans `pk` entier ignorée **+ warning ajouté** ; #15 OCR distant `errors="replace"` (U+FFFD plutôt qu'échec sur octet non-UTF-8) ; #17 `mufi_err` ne compte pas les MUFI *insérés* (dénominateur = MUFI **attendus** de la réf). | **« Vérifier/narrower » → vérifié = ne pas narrower** : narrower serait une régression (FAILED erroné). La valeur est ailleurs : **rationale explicite** + **tests qui verrouillent** le best-effort. Aucun `except Exception: pass` (garde-fou actif). Accepts = **documentés**, pas « corrigés » (comportements voulus). — *preuves : `tests/app/test_jobs.py::{test_publish_failure_does_not_fail_run,test_history_failure_does_not_fail_run}`*. | **Lots A→D + F + G clos.** Reste **E** (cassettes/fixture Gallica réelle) = **hors sandbox** (capture réseau bloquée par l'allowlist → script de capture à exécuter dans un env réseau). |

| D-057 | 2026-06-05 | Lot E — replay déterministe des cassettes (IIIF + HuggingFace) | Le `live` distant n'est pas rejouable en CI (allowlist). D3 : enregistrer de vraies réponses (cassettes) → rejouer **hors-ligne** pour prouver le **parsing réel** (≠ mocks). Script de capture livré (D-054/#39 + fix gzip #40) ; cassettes IIIF + HF capturées par l'utilisateur (Codespace) et commitées (`tests/fixtures/cassettes/{iiif,hf}.json`). | **`tests/adapters/corpus/_cassette.py`** : `replaying(nom)` (contextmanager) branche un `_ReplayTransport` dans la pile `_http` (seam `_make_client`) + neutralise l'anti-SSRF — les **vrais** importeurs tournent sans réseau sur les réponses enregistrées (match `(méthode, schéma, hôte, chemin, query triée)`, FIFO pour URL répétée, `CassetteMiss` si absente). **`test_cassette_replay.py`** : IIIF (manifeste **v3** cookbook → `CorpusSpec` 2 pages + images téléchargées, images-seules) ; HuggingFace (découverte du Hub → datasets `source="api"` parsés). | **Parsing réel prouvé en CI, hors-ligne** : ni mock du transport ni réseau (≠ dette Picarones « bugs masqués par les mocks »). Le replay **réutilise le seam** du script de capture (symétrie capture/replay). Images IIIF = placeholder 1×1 (la cassette teste le **parsing/mapping**, pas la fidélité image — `--placeholder-threshold`). **Gallica `/f{n}/` + eScriptorium différés** : cassettes en attente (Gallica 403 sur IP cloud → capture résidentielle ; eScriptorium privé) — le test `/f{n}/` arrive avec `gallica.json`. — *preuves : `tests/adapters/corpus/test_cassette_replay.py` (2 tests, déterministes)*. | **Lot E partiel** (IIIF+HF rejoués en CI) ; reste **Gallica/eScriptorium** (capture hors sandbox). Remédiation : A→G livrés ; E à compléter à la marge. |

| D-058 | 2026-06-05 | Lot E — Gallica : 2 bugs prod révélés par la capture réelle + bascule OCR sur ALTO | La capture Gallica (IP résidentielle) a buté sur du concret que les mocks masquaient : (1) le **manifeste IIIF renvoyait 403** ; (2) `f{n}.texteBrut` **redirige vers ALTCHA** (anti-bot) en accès automatisé → OCR inutilisable. Décision utilisateur : « pas de demi-mesure ». | **(1) URL manifeste** : `…/ark:/…/manifest.json` → `…/iiif/ark:/…/manifest.json` (le préfixe `/iiif/` manquait → 403 en prod ; le loopback servait une fausse URL → bug non vu). **(2) OCR via ALTO** : `fetch_ocr_text` ne fait plus de `texteBrut` mais récupère l'ALTO officiel `RequestDigitalElement?O={ident}&E=ALTO&Deb={vue}` (`O` = ARK sans le naID) et en **extrait le texte** (`String/@CONTENT` en ordre de lecture) via `formats/alto` — réutilise le parseur ALTO de la couche 2. `_http.fetch_bytes` ajouté (ALTO doit être parsé depuis des **octets** : lxml refuse un `str` à déclaration d'encodage). `texteBrut`/`_looks_like_html` supprimés (rupture nette, un seul chemin). | **Vrais bugs prod corrigés, pas que des tests** : le 403 manifeste cassait la vitrine Gallica ; `texteBrut` était mort (ALTCHA). **No half-measure** : remplacement (pas de fallback fragile). Prouvé sur **vraies pages Gallica** : `tests/fixtures/gallica_alto/{f8,f10}.alto.xml` (*Monsieur de Pourceaugnac*) → extraction (42/45 lignes, « MONSIEUR DE POURCEAUGNAC. », « JULIE. ») ; **mapping `/f{n}/`** prouvé sur loopback réel (f8/f10 aux positions 1/2 → docs `f0008`/`f0010`, pas f1/f2). Fixtures normalisées en UTF-8 (déclaration ISO-8859-1 incohérente avec les octets après re-encodage de capture ; contenu authentique). — *preuves : `tests/adapters/corpus/{test_gallica_parse,test_gallica_alto,test_gallica_local_server}.py`, `tests/app/test_corpus_import_gallica.py`*. | **Lot E complet** (IIIF/HF rejoués + Gallica prouvé sur vraie donnée) ; eScriptorium = cassette optionnelle. **Remédiation A→G + E : livrée.** |

| D-059 | 2026-06-06 | S6.b — form UI d'import distant (relie les endpoints `import/*` à la vitrine) | Les 4 endpoints `/api/corpus/import/{iiif,escriptorium,gallica,huggingface}` existaient (D-044/D-045/D-054) mais **rien dans l'UI ne les atteignait** — surface morte côté vitrine. | **Section « importer un corpus distant » sur `/benchmark`** : `<select>` source (4 options, rendu serveur) + champs par source (rendus serveur, testables) + bouton ; `benchmark.js` étendu (JS léger ES5, en-tête CSRF) POST vers `import/{source}` → `corpus_id` qui **devient le corpus actif du lancement** (même flux que l'upload ZIP). **Masquée en mode public** (`public_mode` passé au `home` router → `benchmark.html` ; l'endpoint renvoie de toute façon 403). Coercition JS des champs (nombre/case/texte ; optionnels vides ignorés). i18n FR/EN. | **Surface serveur testée** (`test_benchmark_page.py` : section + 4 sources + champs `manifest_url`/`ark`/`base_url`/`token`/`dataset_id` rendus ; **masquée en public**) + **syntaxe JS vérifiée** (`node --check`, déjà en CI). Le comportement navigateur (fetch) reste non exécutable en CI (acknown S2/S6) → couvert par : endpoints déjà testés (D-044/45/54) + surface serveur + syntaxe JS. **Réutilise tout** (seam `corpus_id`→launch, `fetchJson`/CSRF de `benchmark.js`). | **S6.b livré** ; reste **segmentation-UI** (nécessite d'abord un backend de run segmentation web — pas qu'un écran). |
| D-060 | 2026-06-06 | S6 — segmentation-UI (squelette de visualisation, bout-en-bout) | Dernière surface S6. Décision de découpe (avec l'utilisateur) : **squelette d'abord, segmenteur après** — on construit **toute l'enveloppe de visualisation** réutilisable (persistance LAYOUT + endpoint image + helper SVG + page), exercée par un layout de **démo** ; le branchement d'un **vrai run de segmentation web** est une **Tranche 2** distincte (≠ « un écran qui ne montre rien »). | **`reports/layout_svg.py`** : `layout_to_svg(layout, *, image_href=None)` — rend la 1ʳᵉ page d'un `CanonicalLayout` en **SVG serveur déterministe** (boîtes de régions + labels, attributs de présentation → CSP-safe, `html.escape`, fond image OU page neutre). **`app/segmentation.py`** : `SegmentationStore` (persiste `layout.json` + image, id `uuid`, **chemin validé** anti-traversal via `validated_path`→`None`) + `demo_layout()` (3 régions title/paragraph/figure) + `demo_page_image()` (PNG **déterministe** encodé stdlib `zlib`/`struct`, pas de PIL). **`interfaces/web/routers/segmentation.py`** : `GET /api/segmentation/{id}/image` (FileResponse, id validé en amont → 404 hors zone/inconnu). **`home.py`** : route `/segmentation` (nav promue *live*, SVG inline + table des régions) ; **`app.py`** sème la démo dans un store par application. i18n FR/EN `seg_*`. | **Anti-dormant tenu** : la graine de démo **exerce réellement** persistance + endpoint image + SVG (le PNG est servi, le `<image href>` pointe l'endpoint) — pas d'API posée « au cas où ». **Sécurité** : id d'URL **validé** (traversal → 404, prouvé `..%2F..` + store unit) ; SVG inline + image `self` conformes à la CSP (`img-src 'self'`). **Déterminisme §12** : `layout_to_svg` et `demo_page_image` bit-à-bit stables (testés). **100 % rendu serveur** (0 JS → testable en CI : page rend les 3 régions + SVG, endpoint sert le PNG, FR/EN). **Découpe assumée** : l'écran tourne sur `demo` (≠ vrai run web) — la **Tranche 2** (run de segmentation branché à un corpus uploadé) réutilisera le **même** `SegmentationStore.save`, l'enveloppe ne bouge pas. — *preuves : `tests/reports/test_layout_svg.py`, `tests/app/test_segmentation_store.py`, `tests/interfaces/web/test_segmentation_page.py`*. | **segmentation-UI (squelette) livrée** — toutes les **pages** S6 sont au design ; reste la **Tranche 2** : brancher un vrai run de segmentation web (corpus → `CanonicalLayout` persisté via le même store). |

| D-061 | 2026-06-06 | T2 — sink LAYOUT (un seul exécuteur, anti-jumeau) | Tranche 2 segmentation. Risque soulevé (utilisateur) : un **service de run segmentation dédié** deviendrait un **second chemin d'exécution** mort quand le pipeline T5 complet arrive. Vérification de `RunResult` (scalaire **par conception** — aucune géométrie) → la géométrie a besoin d'un domicile durable distinct (`SegmentationStore`, D-060), **pas** d'un fork d'exécuteur. | **`orchestrator.run(..., artifact_sink=…)`** : crochet optionnel `ArtifactSink(PipelineOutputs, RunManifest)` invoqué **dans** le bloc workspace (URI encore lisibles), avant `evaluate_run`. **`JobRunner`** gagne `segmentation_store` optionnel + `_persist_layouts` (best-effort) : pour chaque `LAYOUT` produit, relit le `CanonicalLayout` et le `save` dans le store. **`SegmentationStore.latest()`** : id du layout le plus récent (mtime). **`home.py /segmentation`** : affiche `latest()` (run réel > graine démo) au lieu de l'id de démo figé. **`app.py`** : `seg_store` créé avant le runner, passé aux deux (même instance que `/segmentation` lit). | **Un seul exécuteur, un seul format** : la géométrie est un **artefact du run** persisté par un sink, pas un second pipeline ; allonger la spec (seg→OCR→assemble) n'ajoute aucun chemin. **Segmenteur-agnostique** : le sink capte **tout** `LAYOUT` produit → testé en CI avec `precomputed_layout` (0 dép), `pp_doclayout` (T2.1) y passera de même. **Couches tenues** : l'orchestrateur (6) invoque un callback **sans** connaître `SegmentationStore` ; le `JobRunner` (6) fournit le sink → pas d'inversion de dépendance. **Best-effort** : un échec de persistance n'abat pas le run (le `RunResult` est l'output, la viz est secondaire). — *preuves : `test_orchestrator` (sink reçoit un LAYOUT lisible ; run sans sink OK), `test_jobs` (run precomputed_layout → store contient le layout ; sans store → DONE), `test_segmentation_store` (`latest`), `test_segmentation_page` (affiche le run le plus récent)*. | **sink livré** (run → géométrie persistée → `/segmentation` affiche le run réel) ; reste **lancement web d'un run de segmentation** (endpoint/UI) + **statut/gate segmenteur** + **image Space** (PaddleX + poids) |

| D-062 | 2026-06-06 | T2 — endpoint de run segmentation + statut/gate segmenteur | Rendre `pp_doclayout` (T2.1) **déclenchable** : un run de segmentation sur un corpus uploadé, persisté par le sink (D-061), visible sur `/segmentation`. Catégorie **distincte** des moteurs OCR (un segmenteur produit un LAYOUT, pas du texte) → ne pas polluer le `<select>` moteur du lanceur. | **`app/engines.py`** : `segmenter_statuses()` (dispo PP-DocLayout = PaddleX présent ; **jamais masqué en public** — segmenteur local, pas de clé ; sonde injectable). **`interfaces/web/routers/segmentation.py`** : `POST /api/segmentation/run` (`{corpus_id}`, **CSRF**) → `_segmentation_spec` (1 étape `pp_doclayout`, `EvaluationSpec(views=())`) lancé par le **même** `JobRunner` ; gate **409** si segmenteur indisponible. `app.py` câble runner+corpus_store+provider segmenteur. | **Un seul exécuteur** : un segmenteur **est** un pipeline lancé par le `JobRunner` existant → le sink (D-061) persiste son LAYOUT, `/segmentation` l'affiche ; aucun second chemin. **Catégorie séparée** : endpoint dédié (≠ `/api/runs`), `pp_doclayout` **absent** de `engine_statuses` → le `<select>` OCR reste propre (testé). **Public-safe** : segmenteur local sur image uploadée (ni clé, ni SSRF) → **pas** de gate public, contrairement aux moteurs cloud. **Chaîne complète prouvée en CI sans PaddleX** : registre substituant `pp_doclayout` par un détecteur **injecté** → `POST run` → run → sink → `store.latest()` porte le LAYOUT ; + gate 409, CSRF 403, corpus 404, corps invalide 422. — *preuves : `test_segmentation_run.py`, `test_engines.py` (segmenteur dispo/indispo, absent de la liste OCR)*. | **endpoint + statut/gate livrés** ; reste le **bouton UI** sur `/benchmark` (déclenche le run sur le corpus courant) + l'**image Space** (PaddleX + poids PP-DocLayout bakés) |
| D-063 | 2026-06-06 | T2 — bouton UI « Segmenter » sur `/benchmark` | Brancher l'endpoint de run segmentation (D-062) à la vitrine : déclencher un run sur le **corpus courant** (celui d'un upload/import) sans quitter le lanceur. Consommateur du `segmenter_statuses` (D-062) → ferme la boucle UI. | **`benchmark.html`** : bloc « …ou segmenter la mise en page » — bouton `#segment-btn` (rendu serveur, **désactivé** tant qu'aucun corpus n'est sélectionné) si segmenteur dispo ; sinon **motif d'indisponibilité** (`[segment]`). **`benchmark.js`** : `subscribe(jobId, onTerminal)` factorisé (même SSE, un seul exécuteur) ; le bouton POST `{corpus_id}` → `/api/segmentation/run` → suit le run → à la fin, **lien vers `/segmentation`** (≠ lien rapport). `enableSeg()` active le bouton après upload/import. **`home.py`** : `/benchmark` reçoit le statut segmenteur ; `app.py` passe le provider. i18n FR/EN `seg_run_*`. | **Réutilise tout** : même corpus (`corpusId`), même SSE, même exécuteur — le bouton n'ajoute **aucun** chemin de run, juste un déclencheur. **Catégorie séparée tenue** : le segmenteur n'est **pas** dans le `<select>` moteur OCR ; son bouton est distinct, conditionné à `segmenter_statuses`. **Surface serveur testée** (bouton présent si dispo ; **motif `[segment]`** + bouton absent si indispo) + **syntaxe JS** (`node --check`). Le comportement navigateur (fetch/SSE) reste non exécutable en CI (acquis S2/S6) → couvert par l'endpoint déjà testé (D-062) + surface serveur + syntaxe JS. — *preuves : `test_benchmark_page.py` (bouton dispo/indispo)*. | **boucle UI segmentation fermée** (upload/import → Segmenter → run → `/segmentation`) ; reste l'**image Space** (PaddleX + poids PP-DocLayout bakés dans `Dockerfile.space`) pour que le Space appelle réellement le YOLO |
| D-064 | 2026-06-06 | T2.5 — image Space **différée** (décision) | Tranche 2 **fonctionnellement complète** (boucle upload/import → Segmenter → run `pp_doclayout` → sink → `/segmentation`, prouvée en CI). Reste à faire tourner le segmenteur sur le **Space public** : y installer PaddleX + poids. | **Décision (avec l'utilisateur) : différer.** PaddleX (paddlepaddle CPU) est lourd ; les limites de **taille d'image / cold-start du free-tier HF** sont inconnues → on ne baker pas à l'aveugle. Aucune modif de `Dockerfile.space`/`requirements.txt` pour l'instant. | **Dégradé gracieux, pas cassé** : sans l'extra `[segment]`, `segmenter_statuses` renvoie « indisponible (extra [segment]) » → le bouton `/benchmark` affiche le motif, l'endpoint répond **409** ; le reste de la vitrine est intact. **Pas de code dormant ajouté** : on ne pose pas un Dockerfile spéculatif. Reprise quand le tier HF est connu : option A (baker poids au build, reproductible, image lourde) vs B (poids au runtime, plus léger mais réseau + non reproductible) — à trancher à ce moment-là. | **T2.5 au backlog** (déféré) ; **Tranche 2 close fonctionnellement**. Prochain chantier = au choix de l'utilisateur (hors segmentation). |
| D-065 | 2026-06-06 | Remédiation post-audit de migration (Étapes 0-3) + **Bibliothèque = hub de préparation des corpus** (renverse D-059) | Audit de migration (session dédiée) : *Internal Server Error* sur `/library` et `/history` (Space), orchestration fuitée dans les routeurs, dispatch moteur muet (`mistral`→tesseract), corpus invisibles après upload — **et décision produit** : upload + imports appartiennent à la **Bibliothèque**, pas au Banc d'essai (renverse **D-059** + l'invariante « `/library` 0 JS » de D-048 + « UI upload/sélection sur `/benchmark` », `PLAN_SPACE_INTERACTIF` TU2.f.2). | **Ét. 0** garde-fous exécutables `tests/guardrails/` (`xfail(strict)` auto-désarmants). **Ét. 1** stockage runtime inscriptible `app/data_dir.resolve_data_dir` (≠ dossier rapports baké root-only ; `catalog.seed_reports`) ; dégradation réseau à la source (`_http` enveloppe les erreurs `httpx` → `HttpFetchError`, catalogues rattrapent `CorpusHttpError`) + garde `sqlite3.Error` sur `/history` ; mode public auto sur Space (`is_huggingface_space`). **Ét. 2** specs sorties des routeurs → `app/run_planning.py` (`plan_ocr_run`/`plan_segmentation_run`), dispatch exhaustif (refus explicite, plus de retombée silencieuse). **Ét. 3** `CorpusStore.list_corpora` + « Mes corpus » sur `/library` ; **upload ZIP (glisser-déposer) + imports IIIF/Gallica/eScriptorium/HF déplacés `/benchmark` → `/library`** (`static/js/corpus.js`) ; Banc d'essai → `<select>` de corpus existants. | **Décision produit** (product owner) : la Bibliothèque **capture/prépare** les corpus, le Banc d'essai en **sélectionne** un prêt. Doc réconciliée **ici** (le code avait d'abord été poussé sans — dette admise à l'audit). `/library` **porte désormais du JS** (`corpus.js`). **Limites assumées** : interaction navigateur non exécutée en CI (`node --check` + surface serveur testées) ; `/data` pas encore gué sur `SPACE_ID` ; déploiement (`Dockerfile`/README) pas encore aligné. | PR #54 (tous commits, CI multi-OS verte) — merge sur vert ; **reste** : Bibliothèque-flux (datasets HF curatés, standard à définir : capture → télécharger **ou** OCRiser en flux) · déploiement (data-dir persistant, README, libellé « lecture seule ») · tests transport `httpx` + `plan_ocr_run` tesseract · CSS dropzone |
| D-066 | 2026-06-10 | **T8 — consolidation parité** (1ʳᵉ tranche de [`PLAN_PARITE.md`](PLAN_PARITE.md)) : enveloppe « ressources » (E1) livrée, enveloppe « analyses » (E2) **documentée mais champ différé**, hygiène docs | Le plan de parité exige 2 extensions d'enveloppe avant toute surface. **E1** : `Module.execute` → `StepOutput(artifacts, usage)` (changement **atomique**, 10 builders + fanout + plugin de réf dans le même commit, jamais 2 signatures) ; l'exécuteur **mesure** les durées (source unique), les adapters LLM/VLM remontent `tokens_in/out` (OpenAI/Anthropic/Mistral `usage.*`, Ollama `prompt_eval_count`/`eval_count`) ; agrégat par (pipeline × document) trié → `RunResult.usage`, `schema_version=2` (un JSON v1 sans `usage` se recharge). **E2** : une union discriminée **vide** n'est pas implémentable et un champ sans payload violerait « pas de consommateur = supprimé » → le **contrat** (3 règles dures) est gravé dans la docstring de `result.py` + `PLAN_PARITE.md` amendé ; le champ `analyses` naît en T9 avec son 1ᵉʳ payload (`inference`). **Hygiène** : 2 constats sécurité de l'audit (rate-limit asymétrique, fuite IP) = **faux positifs** (middleware global + `_prune` déjà livrés) ; `NEXT_SESSION.md` réécrit en pointeur mince + `test_status_freshness` étendu ; ~35 annotations de tranche purgées des docstrings (les réfs `D-0xx` au journal, durables, restent). **Abandons actés** (PLAN_PARITE §2) : robustesse synthétique, clustering/corrélations/Venn, readability/lexical_modernization/equivalence_profile/error_absorption/levers/specialization/inter_engine/baseline_comparison/module_policy/modern_archives/rare_tokens ; Pero/Google/Azure/Calamari first-party (voie plugin). | Durées = wall-clock, **métadonnée d'environnement** comme les horodatages du manifeste : exclues de l'identité des artefacts, non rendues dans le rapport (jusqu'à T10, qui arbitrera leur rendu). | `make ci` complet vert ; suite + archi + goldens régénérés |
| D-067 | 2026-06-10 | **T9 — verdict statistique complet** : Nemenyi + bootstrap portés, champ `analyses` né avec son 1ᵉʳ payload | Le scalaire `significance_p` (Wilcoxon/Friedman brut) dit « une différence existe » mais sur-affirme à comparaisons multiples ; le verdict produit (`synthesis`) doit être corrigé. | `evaluation/inference.py` (maths pures stdlib — déterminisme indépendant de numpy/BLAS ; plancher `MIN_SUPPORT=6` devenu **source unique**, importé par `metrics/stats`) + `evaluation/analysis.py` (E2 : `Analysis{scope,view,pipeline?,document_id?}` + `InferencePayload` discriminé par `kind` — l'union s'élargit d'un membre par famille) ; runner : mêmes séries alignées que la passe inter-moteurs (un calcul, deux lectures) ; tri (view, kind, metric). `schema_version` reste **2** : v2 est née sur cette branche, jamais publiée — pas de bump pour un ajout additif au sein de la même livraison. Parité numérique **prouvée** contre l'implémentation source exécutée sur les mêmes données (pas recopiée de tête). | À k=2 le post-hoc n'a pas de sens (pas de correction à faire) → payload sans CD, verdict = p Wilcoxon ; le CDD SVG reste **optionnel différé** (consommateur académique pas encore demandeur). | `make ci` complet vert |
| D-068 | 2026-06-10 | **T10 — économie** + règle d'ancrage des tests de port + arbitrage « rapport déterministe vs mesures environnementales » | (1) Porter l'économie sans graver les bugs de Picarones ; (2) la démo golden octet-stable exécute le run **deux fois** — or durées/jetons (E1) sont du wall-clock, non reproductibles. | (1) **Règle PLAN_PARITE §5.8** : algo à référence externe → parité + référence publiée ; **heuristique maison → valeurs de test dérivées à la main** sur micro-fixtures, jamais générées en exécutant Picarones (la source n'est pas un oracle de justesse). L'économie est portée sous ce régime : modèle de coût relu et simplifié (jetons **mesurés** × €/MTok au lieu de la conversion tokens→pages estimée de la source — plus juste grâce à E1). (2) Le rapport reste une **fonction pure du `RunResult`** (testé bit-à-bit sur même entrée) ; la **démo** filtre les canaux environnementaux (`usage`, payload `economics`) avant rendu — elle vitrine le déterminisme du contenu évaluatif ; un run réel rend l'économie. (3) `pricing.json` (pas yaml : whitelist archi couche 3 non élargie) ; péremption évaluée vs `completed_at` du manifeste (pas d'horloge au rendu). | Multi-étages cloud dans un même pipeline : jetons E1 agrégés par document, non ventilables par modèle → coût `None` + motif explicite (limite documentée, levée si un consommateur exige la ventilation par étape). Tarifs = donnée indicative datée, éditable. | `make ci` complet vert |
| D-069 | 2026-06-10 | **T11 — diagnostic d'erreurs** : « voir où ça casse », texte à l'appui | Le CER dit *combien* ; il faut *où* et *quoi* (choix de modèle/langue, relecture ciblée). | `metrics/diagnostics.py` (2 `@document_metric` texte pur : recall tolérant rapidfuzz, ancrage trigrammes stdlib) + `evaluation/diagnostics.py` (`DiagnosticsCollector` branché dans `_score_document` — le contexte **texte déjà chargé/normalisé** est réutilisé, aucun rechargement ; confusions via `difflib.SequenceMatcher` apparié positionnellement ; pires lignes appariées par index, top-5, extraits ≤ 160 chars). Le payload porte des **extraits verbatim** (contenu court borné dans `RunResult` — assumé : c'est la donnée du rapport, échappée au rendu). Diagnostics **déterministes** (dérivés du texte) → la démo les rend sans filtrage, contrairement à `economics`. | Lignes appariées par index (pas d'alignement de lignes flottant — suffisant pour des pages OCR ; à revisiter si un corpus à lignes réordonnées apparaît). Sous-tranche philologie étendue différée. | `make ci` complet vert |
| D-070 | 2026-06-10 | **T12 — confidences & calibration** : l'artefact `CONFIDENCES` devient consommé, `ConfidenceToken` né en domain | « Peut-on faire confiance au score du moteur ? » — sans calibration, une confiance moteur est un chiffre décoratif. | `domain/confidence.py` (type pur, backlog §domain coché) ; tesseract : 2ᵉ appel `image_to_data` dédié (le texte reste produit par `image_to_string` — ne pas changer la sortie OCR existante), sidecar JSON + artefact `CONFIDENCES` ajouté à `output_types` (les steps déclarant `(RAW_TEXT,)` restent valides : l'exécuteur vérifie les sorties **déclarées**, les surplus enrichissent le pool) ; `evaluation/calibration.py` : ECE/MCE (référence publiée Guo 2017) sur paires (confiance, correct), « correct » = appartenance exacte au multi-ensemble des mots de la GT — proxy **maison** assumé (pas d'alignement positionnel : un mot juste déplacé reste retrouvable), payload `calibration` 4ᵉ membre de l'union. ECE/MCE restent **dans le payload** (diagnostic), pas en `MetricScore` : la calibration ne classe pas les moteurs, elle qualifie leurs auto-estimations — un scalaire d'historique viendra avec son consommateur. | Coût : un 2ᵉ sous-processus tesseract par document (mesuré honnêtement par E1). Le proxy d'exactitude sur-note les mots fréquents (multi-ensemble) — limite documentée, à durcir par alignement si un consommateur l'exige. | `make ci` complet vert |
| D-071 | 2026-06-10 | **T13 — taxonomie d'erreurs** par règles pures ; classes à données externes élaguées | Un CER agrégé ne dit pas *quoi corriger* (modèle ? normalisation ? casse ?) ; la source classait via Levenshtein mot-à-mot + règles, mais 3 classes exigeaient des **données** (lexiques pour `hapax`, charset moteur pour `oov_character`, table MUFI pour `abbreviation`). | `evaluation/taxonomy.py` : 8 classes par règles **déterministes** (priorité : segmentation → case → diacritic → ligature → visual → résiduel ; lacunes/insertions au niveau alignement difflib). La canonisation visuelle (rn→m, u→n…) ne s'applique qu'aux paires **déjà fautives** — pas de faux positif sur un mot juste. Payload `taxonomy` 5ᵉ membre de l'union ; collecte au fil du scoring (zéro relecture). Les 3 classes à données ne sont **pas** portées (« pas de consommateur = supprimé » s'applique aussi à la donnée absente) ; T13.b NER différé (extra spaCy, valeur réelle mais lourd). | La paire fusion/fragmentation compte les mots **produits** (2 pour « bon jour ») — convention documentée, dérivée à la main dans les tests. | `make ci` complet vert |
| D-072 | 2026-06-10 | **T14 — moteurs HTR & cloud** : Kraken (manuscrit) + Mistral OCR (1ᵉʳ moteur facturé à la page) | Tesseract seul ne couvre pas la mission HTR ; et sans moteur payant réel, l'axe coût du Pareto restait théorique. | Kraken : pattern tesseract (lazy import, invoke `pragma no cover` mockable, sidecar `CONFIDENCES` — confiance d'un mot = moyenne de ses caractères, dérivée à la main en test) ; `model` obligatoire, pas de poids embarqué (taille/licence). Mistral OCR : kind séparé `mistral_ocr` (le kind `mistral` = LLM), facturation **à la page** → extension `cloud_page_kinds` du modèle de coût T10 (cumul machine + jetons + pages ; basis suffixé `+pages`). Sécurité : modèle actuel « pas de masquage public » (la protection = visibilité du Space + clé) s'applique tel quel. | Le run kraken réel exige binaire + modèle → test `live` différé ; Calamari reste la démo de plugin **out-of-tree** (autre dépôt). | `make ci` complet vert |
| D-073 | 2026-06-10 | **T15 — reprise d'exécution** ; différé-par-design **levé sur décision produit** (demande utilisateur explicite) | Les runs cloud/longs coûtent ; relancer pour une métrique ajoutée ré-OCRisait tout. | Cache **adressé par contenu** (≠ journal NDJSON de la source) : la clé d'unité couvre tout ce qui influence l'exécution (code, spec, params, octets de l'image) et **rien** de ce qui ne l'influence pas (la GT — l'évaluation est recalculée à chaque run, aucun score en cache). `usage` d'origine restauré (un coût de relecture de cache serait un mensonge économique). Périmètre : opt-in (orchestrateur + CLI) ; pas câblé au web (consommateur Space à confirmer). | Le cache ne borne pas sa taille (purge manuelle du dossier) — limite documentée, gc à la demande d'un consommateur. | `make ci` complet vert |
| D-074 | 2026-06-10 | **S7.a — surfaces utilisateur** : CSV, CLI `history`, profils dynamiques ; S7.b (glossaire + prompts) **différé** à une session dédiée | Les utilisateurs tableurs et l'usage CLI de l'historique n'avaient pas de surface ; l'endpoint profils manquait (et Picarones avait appris qu'une liste statique pourrit). | `reports/csv_export.py` (couche 7, lecture pure — `repr(value)` préserve la précision flottante) ; CLI `history` lit `HistoryStore` (série chronologique ou régressions Δ signé) ; `app.engines.normalization_profiles()` reflète `NORMALIZATION_PROFILES` à l'appel — un profil ajouté en couche 2 apparaît sans câblage. | Glossaire et prompts = données volumineuses à élaguer entrée par entrée → tranche dédiée plutôt qu'un port en masse (garde-fou §5.5). | `make ci` complet vert |
| D-075 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 1 — le Space exécute un vrai OCR gratuit** : image moteur + mode public fail-closed + binaire tracé | Le Space public était une vitrine lecture seule : aucun moteur ne tournait, le visiteur ne pouvait pas faire un vrai OCR sans clé/installation (manque n°1 vers 1.0). | **`deploy/Dockerfile.engine`** (nouveau, racine-plate) bake Tesseract 5 + langues `fra/lat/eng`, fixe `TESSDATA_PREFIX` + **`OMP_THREAD_LIMIT=1`** (garde anti-deadlock OpenMP free-tier, leçon Picarones) + `XEROCR_PUBLIC_MODE=true`, **smoke-test au BUILD** (`--version` + `--list-langs \| grep -qx fra` + OCR d'une image générée, **timeout=124 → build refusé**), `USER` non-root ; `deploy/requirements.txt` **+`pytesseract`** (inerte dans la vitrine, effectif avec le binaire baqué). **Mode public câblé fail-closed** : `app.engines.PUBLIC_ENGINE_KINDS = {precomputed, tesseract}` (source unique) ; `runs.py` refuse **403** tout kind hors socle gratuit **avant** toute vérif de clé/dispo (cloud jamais tenté sur instance publique, même clé posée), `create_app` propage `public_mode` au lanceur ; plugins déjà gated (`discover_plugins(enabled=not is_public)`). **Tesseract exposé** dès que le binaire est présent (détection `shutil.which`, aucun masquage). **Déterminisme** : hook de provenance **`TesseractAdapter.system_binaries()`** (duck-typing hors `Module` Protocol) → l'orchestrateur capte `tesseract --version` dans **`RunManifest.system_binaries_lock`** (ferme le `non couvert ici` de `_manifest`). **Workflow** `deploy-space.yml` : déploie l'image **moteur** (≠ vitrine), build (smoke build) + **smoke OCR réel** (texte ImageMagick → OCR `fra` dans le conteneur) avant push. | **Un seul contrat** tenu : `system_binaries()` est un hook de *provenance* optionnel, pas un 2ᵉ contrat d'exécution (le `Module` Protocol reste intact). **Segmenteur PP-DocLayout (T2.5) reste différé** (décision §Étape 1 : mesurer le cold-start d'abord ; dégradé gracieux déjà livré, ne bloque pas 1.0). Allowlist (≠ denylist) : un futur moteur est gated par défaut. | confirme le verdict `system_binaries_lock` « capture live » (couche 1/6 docs) ; ouvre la route 1.0 (étapes 2→5 inchangées) |

| D-076 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 2a — Google Vision first-party** (1ʳᵉ tranche de la parité moteurs ; 2b Azure · 2c Pero/Calamari plugins · 2d vérifs · 2e prompts = sessions suivantes) | La parité moteurs exige Google Vision en first-party (arbitrage acté §Décisions). | **`adapters/ocr/google_vision.py`** : `GoogleVisionAdapter` (`IMAGE → RAW_TEXT`) via l'**API REST** (`httpx` paresseux) **plutôt que le SDK gRPC** `google-cloud-vision` (lourd, auth ADC, non rejouable) — transport léger, **cassette-testable**, auth **clé** `GOOGLE_VISION_API_KEY` ; `DOCUMENT_TEXT_DETECTION` → `fullTextAnnotation.text`. **Contrat unique** tenu : implémente le `Module` Protocol directement (aucun double contrat). Câblage : extra **`[google]`** (= httpx, comme `ollama`), builder `_build_google_vision` (+ registre), sonde `_google_vision_status` (httpx+clé → dispo ; sinon **indisponible propre**), `_OCR_ENGINES` (run-planning) ; **fail-closed public** (hors `PUBLIC_ENGINE_KINDS` → 403, hérité de D-075). **Anti-silence économie** : un moteur cloud facturé à l'image NE doit pas retomber en « temps machine seul » → entrée **`cloud_page_kinds.google_vision` = 1.38 €/1k** (≈ liste Vision 1.50 $/1k, même base de conversion que `mistral_ocr`) dans `pricing.json`, **livrée avec l'adapter** (feature entière). | Test **cassette** (`httpx.MockTransport`, réponse Vision authentique) : parsing + requête bien formée + erreurs API/HTTP, valeurs **dérivées main** ; coût **dérivé main** depuis la table packagée. **`lang`** du planificateur **ignoré** (Vision auto-détecte ; pas de mapping ISO-639-3→BCP-47 fragile). **Différé (par design, 2d)** : remontée `tokens_in/out` (Vision facture à l'image, pas au jeton → `cloud_page_kinds`, déjà couvert). | ouvre la parité moteurs (T14 avait livré Kraken + Mistral OCR ; reste Azure 2b + plugins 2c) |

| D-077 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 2b — Azure Document Intelligence first-party** (2ᵉ tranche parité moteurs ; reste 2c plugins · 2d vérifs · 2e prompts) | La parité moteurs exige Azure DI en first-party (arbitrage acté §Décisions). | **`adapters/ocr/azure_di.py`** : `AzureDocIntelAdapter` (`IMAGE → RAW_TEXT`) via l'**API REST** (`httpx` paresseux) plutôt que le SDK `azure-ai-documentintelligence`. **Flux asynchrone modélisé fidèlement** : `POST …prebuilt-read:analyze` → `202` + en-tête `Operation-Location` → **sonde** (GET) jusqu'à `status=="succeeded"` (`failed`/délai → erreur), puis `analyzeResult.content`. Auth **clé** `AZURE_DOC_INTEL_KEY` (en-tête `Ocp-Apim-Subscription-Key`) + `AZURE_DOC_INTEL_ENDPOINT` (config opérateur → endpoint de confiance, pas de durcissement anti-SSRF). **Contrat unique** : `Module` Protocol direct, aucun double contrat. Câblage : extra **`[azure]`**, builder `_build_azure_di`, sonde `_azure_di_status` (httpx+endpoint+clé), `_OCR_ENGINES`, fail-closed public hérité (D-075). **Anti-silence économie** : `cloud_page_kinds.azure_di = 1.38 €/1k` (≈ liste Read 1.50 $/1k, même base que google_vision/mistral_ocr), **livrée avec l'adapter**. | Test **cassette** (`httpx.MockTransport`) : flux complet POST→sonde→extract + **polling running→succeeded** (`sleep` injecté no-op, **aucune attente réelle**) + erreurs `failed`/HTTP/Operation-Location absente, valeurs **dérivées main** ; coût dérivé main. `lang` du planificateur **ignoré** (Read auto-détecte). `transport`+`sleep` injectables → sonde déterministe sans réseau. | poursuit la parité moteurs (D-076 = Google Vision) ; reste Pero/Calamari plugins (2c) |

| D-078 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 2c — Pero + Calamari** : **décision révisée** sur demande utilisateur — *in-tree first-party* (≠ « plugins hors-dépôt » du plan initial) | L'utilisateur a relevé l'**incohérence** : Google/Azure/Kraken sont in-tree first-party, pourquoi Pero/Calamari seraient hors-dépôt ? Clarification : il n'y a **aucune discrimination de contrat** (un seul `Module` Protocol pour socle ET plugins) ; la seule différence = livraison + le fait que les plugins tiers sont **désactivés en mode public** (sécurité, code tiers in-process). L'argument « plugins pour prouver le seam » tombe : le chemin entry-points est **déjà prouvé** (D-034, test paquet pip réel). | **`adapters/ocr/pero.py`** + **`adapters/ocr/calamari.py`** : `PeroAdapter`/`CalamariAdapter` (`IMAGE → RAW_TEXT`), **calqués sur Kraken** — locaux, sans clé, **`model` obligatoire** (config PERO / checkpoint Calamari), lib lourde en **extra** (`[pero]`=`pero-ocr`, `[calamari]`=`calamari-ocr`), `_invoke_*` paresseux (live/`# pragma: no cover`), **non déployés au Space** (absents de `PUBLIC_ENGINE_KINDS` → gated). Câblage : builders (`model` requis) + registre, sondes `_pero_status`/`_calamari_status` (présence lib → dispo, comme Kraken), `_OCR_ENGINES`. **RAW_TEXT seul** (pas de mapping de confiances fragile — minimal). **Pas de pricing** : locaux → coût machine par défaut (≠ cloud ; pas d'anti-silence). **`docs/PLUGINS.md`** : documente le seam public (Protocol + entry-points) — reste la voie pour les **vrais** tiers (YOLO HF, module local expérimental). | **Cohérence** restaurée avec Kraken/cloud, **zéro dépôt externe**. Coût assumé : 2 extras lourds dans la matrice — mitigé (tests **mockés**, deps **hors CI**, non déployés, fail-closed public). Lançabilité web avec `model` = même limite que Kraken (modèle non passé par `run_planning` ; exposition du champ = étape 3c, **non touchée ici**). | révise le verdict « plugins hors-dépôt » du plan (2c) ; clôt la parité moteurs côté adapters (reste 2d vérifs + 2e prompts) |

| D-079 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 2d — vérifs** : zero-shot bout-en-bout + jetons cloud testés (reste 2e prompts) | Tranche de **vérification** : (1) le mode `zero_shot` n'avait pas de test E2E ; (2) la **cartographie usage→jetons** de chaque adapter cloud vivait dans le client live (`# pragma: no cover`) → **non testée**, alors qu'elle alimente l'économie (coût réel par jeton). | **Extract-method behavior-preserving** : `_completion_from_chat` (openai, mistral — `prompt_tokens`/`completion_tokens` + `choices`) et `_completion_from_message` (anthropic — `input_tokens`/`output_tokens` + `content` blocs) **sortis** du client live vers une fonction **pure** (aucun réseau/SDK) → la cartographie est désormais **couverte et testée sur réponse de cassette** (`SimpleNamespace` à la forme du SDK). `tests/adapters/test_llm_token_usage.py` : 3 fournisseurs + usage absent → `None` (jamais 0 inventé) + choices vide → jetons conservés. **Zero-shot E2E** : `tests/pipeline/test_zero_shot_e2e.py` — `plan_benchmark_run` (Competitor `openai`/`zero_shot`) → spec **1 étage IMAGE→RAW_TEXT** → orchestrateur (vision invoke mocké) → CER 0 **et** jetons VLM (7/3) remontés dans `RunResult.usage`. | **Zéro changement de comportement** (refactor d'extraction) ; le client live reste `# pragma: no cover` (réseau). Ollama (local, gratuit) hors scope « cloud » — ses jetons ne pèsent pas le coût (machine-time). | clôt 2d (vérifs) ; la parité moteurs est complète sauf **2e** (16 prompts curés) |

| D-080 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 2e — prompts curés par période + prompt libre dans l'UI** (clôt l'étape 2) | (1) Les 16 prompts curés de Picarones manquaient ; (2) l'utilisateur veut pouvoir **écrire son propre prompt** dans l'interface (le textarea `#draft-prompt` existait déjà et fonctionnait → besoin (2) déjà couvert ; reste à exposer les curés). | **`xerocr/prompts/`** (nouveau paquet **données**) : 16 `.txt` portés (13 correction + 3 zero-shot ; médiéval FR/EN, imprimé ancien, presse XIXe FR/EN/DE/européenne) **+ loader pur** (`available_prompts`/`load_prompt`, lus **dynamiquement** comme les profils de normalisation ; `PromptError` si inconnu). **Adaptation de port** : placeholder `{ocr_output}` (Picarones) → **`{ocr_text}`** (convention `build_prompt` XerOCR) sur les 13 correction, sinon l'OCR ne s'injecterait jamais. **Sélection au plan** : `Competitor.prompt_name` (curé, par nom) **mutuellement exclusif** avec `prompt` (libre, **prioritaire**) ; `_resolve_prompt` en `run_planning` résout le nom → texte (inconnu/les-deux → `RunPlanningError` 422). **UI** : `app.engines.curated_prompts()` (dynamique) → `<select>` curé dans le formulaire **à côté** du textarea libre existant ; `benchmark.js` envoie `prompt_name` (sauf si texte libre saisi → lui prioritaire). package-data `xerocr.prompts/*.txt`. | **Donnée, pas surface exécutable** (comme les profils de normalisation) — paquet leaf (loader pur stdlib+domain, garde-fou `test_prompts_imports_are_pure`). Choix mode↔prompt = responsabilité utilisateur (nommage explicite `correction_*`/`zero_shot_*`) ; pas de validation mode↔prompt (spéculatif). Le **textarea libre était déjà câblé** → l'ajout net = curés + sélecteur. | clôt l'**étape 2 (parité moteurs)** ; reste étape 3 (parité interface/rapport) |

| D-081 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3a (1ʳᵉ sous-tranche) — comparer 2 runs côté client** dans le rapport autonome (reste 3a : badges A→E · hash-router/clavier · nombres FR/EN · palette daltonien) | Le rapport ne permettait pas de comparer deux runs ; la voie server-side (`reports/compare.py`, CLI `xerocr compare`) existait, pas l'interactif **dans** le rapport. | **`reports/_assets/compare.js`** (script **statique**) + **`reports/compare_widget.py`** : bouton + données CER du run courant en **bloc `application/json` inerte** (échappé `<>&` anti-rupture `</script>`) + script **inliné** (autonomie). Le visiteur charge un 2ᵉ `RunResult` JSON de son disque (**FileReader, plafond 50 Mo, zéro réseau**), deltas CER par moteur dans un **bandeau sticky** (régression clay / amélioration fern) ; tout texte issu du fichier passe par `textContent` (**anti-XSS**). **Contrainte CSP résolue proprement** : `script-src 'self'` (sans `'unsafe-inline'`) bloquait l'inline sur la vitrine → **empreinte sha256** du script statique ajoutée à `script-src` **des seules réponses `/reports/`** (`get_csp_policy(path)`), `'unsafe-inline'` reste exclu partout. `render_document(footer=…)` injecte le widget ; la voie server-side reste sans widget. package-data `_assets/*.js`. | **Autonomie préservée** (script inliné, voyage dans le doc) ; **déterminisme** (script constant → hash constant ; données dérivées du `RunResult`). Comportement navigateur non testable en CI → surface testée (bouton/data/script présents · hash = sha256 du fichier · échappement · CSP `/reports/` porte le hash, l'accueil non · `node --check`). i18n du bandeau = **FR** (le rapport est FR ; le FR/EN des nombres est une sous-tranche 3a séparée). | ouvre l'étape 3 (parité interface) ; reste 4 sous-tranches 3a + 3b–3e |

| D-082 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3a (2ᵉ sous-tranche) — badges moteur A→E** (reste 3a : hash-router/clavier · nombres FR/EN · palette daltonien) | Les sections du rapport affichaient le nom des moteurs sans **repère visuel** : pas d'identité « moteur A » partagée entre classement / par-document / crosses. | **`reports/engine_badges.py`** (**source unique**) : `engine_letter`/`engine_accent` (lettre A→Z + accent cyclique fern/slate/clay/butter/ink, oklch), `engine_order` (nom→index par **première apparition** dans `result.pipelines` → **identité stable, pas un rang**), `engine_cell` (badge + nom **échappé**). Consommé par `by_engine`, `by_document`, `cross_engine` (chacun calcule le **même** ordre canonique depuis `result.pipelines` → « A » désigne le même moteur partout, indépendamment du tri d'affichage). CSS `.eng-badge` ajoutée à `html.py`. | **Server-side, zéro CSP/JS** (≠ compare). **Déterministe** (ordre = première apparition → octet-stable). **Anti-divergence** : toute extension de palette se fait dans le seul `engine_badges.py` (les sections ne recopient pas la palette — convention Picarones). Tests : cyclage lettre/accent · ordre première-apparition · cellule échappe un nom hostile · présence dans les sections + le rapport complet. | poursuit 3a ; reste hash-router/clavier · nombres FR/EN · palette daltonien, puis 3b–3e |

| D-083 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3a (3ᵉ sous-tranche) — deeplinks + sommaire + ARIA** (la moitié **server-side** de « hash-router + deeplinks + navigation clavier (ARIA) ») | Le rapport était un long document sans repères : pas d'ancres de section, pas de sommaire, pas de landmarks pour lecteurs d'écran. | **`renderer.py`** : chaque section rendue devient une **région ancrée** `<section id="r-<name>" aria-label="<libellé FR>">` ; un **sommaire** `<nav class="report-toc" aria-label="Sommaire">` en tête relie les régions par **ancres natives** (deeplinks `#r-<name>`, **zéro JS**), affiché dès ≥ 2 sections. Libellés FR via `_SECTION_LABELS` (repli = nom brut). CSS `.report-toc`/`.r-block` (`scroll-margin-top`) dans `html.py`. | **Server-side, zéro CSP/JS** — la navigation clavier (j/k) restera une sous-tranche JS (réutilisera le hash-CSP `/reports/`). **Déterministe** (ordre des sections = ordre du renderer). a11y : `nav`/régions = landmarks. Tests : sommaire masqué < 2 sections, deeplinks ↔ ids, ARIA, repli de libellé, présence dans le rapport complet. | poursuit 3a ; reste navigation clavier (JS) · nombres FR/EN · palette daltonien, puis 3b–3e |

| D-084 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3a (4ᵉ sous-tranche) — navigation clavier + palette daltonien** ; **clôt l'interactivité JS de 3a** (reste hors 3a-JS : voir note FR/EN ci-dessous) | Deux features 3a JS-dépendantes manquaient ; elles partagent la plomberie « script inliné + hash CSP ». | **`reports/_assets/report.js`** (statique, inliné) : **navigation clavier vim** `j`/`k` entre régions `.r-block` (PAS les flèches → défilement natif préservé ; ignore les champs de saisie ; focus du titre pour l'a11y) + **palette daltonien** (`?palette=cb` → classe `.palette-cb` sur `<html>` → CSS bascule `--fern` et les deltas du bandeau vers une paire **bleu/orange** distinguable ; les badges portent une **lettre** → identifiables sans couleur). **`reports/embedded.py`** (**source unique**) centralise les scripts inlinés (`EMBEDDED_SCRIPTS`) + leurs **hashes CSP** ; `get_csp_policy('/reports/')` épingle **tous** les hashes (compare + report), `'unsafe-inline'` toujours exclu. `compare_widget` délègue à `embedded`. | **Progressif** : sans JS, le rapport reste lisible (sommaire à ancres natives). **Déterministe** (scripts statiques → hashes constants). **Anti-divergence** : ajouter un script du rapport = l'ajouter à `EMBEDDED_SCRIPTS` (CSP suit). Tests : `node --check` report.js · report.js fait nav+palette · hashes couvrent chaque script · CSP `/reports/` porte les deux hashes · CSS palette présente. **Note FR/EN** : « formatage des nombres FR/EN » du plan suppose une **i18n complète du rapport** (tout le texte est FR) → bien plus large que 3a ; à planifier à part (cf. 3e glossaire), pas crammé ici. | **3a clôturée côté interactivité** (compare · badges · deeplinks/ARIA · clavier · palette) ; le FR/EN-i18n est sorti du périmètre 3a (note ci-dessus) → 3b ensuite |

| D-085 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3b (1ʳᵉ sous-tranche) — drill-in diff GT↔hypothèse** ; **galerie d'images BLOQUÉE par l'autonomie** (décision d'enveloppe à prendre) | 3b = « galerie miniatures + drill-in diff ». Le drill-in diff est faisable sur des données **déjà présentes** (les `worst_lines` du payload `diagnostics` portent `reference`/`hypothesis` verbatim) ; la galerie d'**images**, non — `RunResult` ne porte aucune image. | **`reports/text_diff.py`** : `char_diff(reference, hypothesis)` aligne via `difflib.SequenceMatcher` (stdlib, déterministe) → `(ref_html, hyp_html)` avec **suppressions** `<del class="d-del">` (en GT) et **insertions** `<ins class="d-ins">` (produites) ; segments **échappés** avant marquage (anti-XSS). Intégré aux **pires lignes** de la section `diagnostics` (remplace le texte brut). CSS `td.diff`/`del`/`ins` + bascule palette daltonien. **Zéro JS**, server-side, octet-stable. | **Galerie images = décision d'enveloppe ouverte** : l'autonomie du rapport (HTML standalone) interdit de pointer des fichiers externes ; embarquer des miniatures data-URI gonfle le rapport. À trancher avec l'utilisateur (embarquer des miniatures ? accepter le poids ? se limiter au drill-in texte ?) avant de l'implémenter — **pas hacké ici**. Tests : `char_diff` (égal/subst/suppr/insert/échappement/déterminisme) + diff dans la section. | ouvre 3b côté texte ; reste **galerie images** (décision) + diff mot-à-mot éventuel |

| D-086 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3b (2ᵉ sous-tranche) — galerie de documents « à la Picarones »** ; **clôt 3b** sur le périmètre sans image | Décision utilisateur (galerie synthétique). **Vérifié sur la source Picarones** : sa galerie n'embarque PAS les images par défaut (aperçu CSS synthétique par strate ; images réelles = opt-in base64/externalisé). Donc une galerie est faisable **maintenant** sans toucher `RunResult`. | **`reports/sections/gallery.py`** : `DocumentGallerySection` (`name="documents_gallery"`) — une **carte par document** (`RunResult.documents`, vue primaire) : **aperçu de page synthétique** (lignes monochromes sur `--paper`, **charte du rapport**, pas de placeholder coloré — le travers Picarones évité), id échappé, **CER par moteur** via les badges A→E (meilleur du doc surligné en `--fern`). CSS `.doc-grid`/`.doc-card`/`.doc-preview` n'utilise **que** les tokens de design existants (gris chauds, rayons, polices, fern). Enregistrée au renderer (après `by_document`) + libellé sommaire « Galerie ». | **Design sur la charte** (zéro couleur ajoutée/héritée — réponse au point faible Picarones). Server-side, **zéro image, zéro changement d'enveloppe**, autonome, octet-stable. **Fac-similés réels = opt-in séparé** (canal images base64 au générateur — non fait, décision ultérieure). Tests : carte/doc, CER+badges, meilleur surligné, id échappé, déterminisme, None sans documents. | **3b clôturée** (diff D-085 + galerie D-086) ; reste 3c (champs de formulaire — dont `model`), 3d, 3e |

| D-087 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3c (1ʳᵉ sous-tranche) — champ `model` des moteurs OCR** ; **referme le gap 2c** (kraken/pero/calamari/mistral_ocr inlançables par le web) | En 2c, les moteurs OCR à modèle ont été livrés in-tree, mais le champ `model` était **masqué en mode OCR-seul** (`data-show` sans `ocr_only`) et `run_planning` ne transmettait pas `comp.model` aux moteurs OCR → ces moteurs échouaient à la construction (modèle requis absent). | **`run_planning`** (chemin OCR-seul) : `if comp.model → ocr_kwargs[name]["model"] = comp.model`. En OCR-seul, `model` = **le modèle du moteur** (chemin `.mlmodel` kraken, config PERO, checkpoint Calamari, nom Mistral OCR) ; tesseract/google/azure l'ignorent (kwarg surnuméraire). **En chaîne, `model` reste le LLM aval** (`_llm_kwargs`) — pas touché. **Form** : le champ « Modèle » (`#draft-model`) gagne `ocr_only` dans `data-show` ; `benchmark.js` (branche `ocr_only`) envoie `model`. | **Ciblé OCR-seul** (l'ambiguïté OCR-vs-LLM `model` en chaîne reste : un `ocr_model` séparé serait spéculatif → différé). `interfaces` reste mince (la résolution vit en `run_planning`). Tests : kraken OCR-seul reçoit son modèle **et se construit** ; sans modèle → kwarg omis ; champ visible en `ocr_only`. | poursuit 3c ; reste `/api/models/{provider}` · preview normalisation · config save/load · `char_exclude`/profil métrique/expose-ALTO |

| D-088 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3c (2ᵉ sous-tranche) — `/api/models/{provider}` + suggestions modèle au formulaire** | Le champ « Modèle » n'avait de suggestions que pour ollama/mistral (datalists live) ; openai/anthropic n'en exposent pas sans clé, et la **capacité vision** (mode `zero_shot`/`text_and_image`) n'était indiquée nulle part. | **`app/models.py`** (source unique) : `provider_models(provider) → tuple[ModelInfo(name, vision)]` — liste **canonique** curée + drapeau **vision** (openai/anthropic/mistral) ; fournisseur inconnu → `()` (fallback gracieux, champ libre). Concern **distinct de `pricing.json`** (pas de couplage). **`GET /api/models/{provider}`** (routeur engines, lecture seule, 200 + liste vide si inconnu). **`benchmark.js`** : pour openai/anthropic, **fetch** `/api/models/{provider}` (même origine, CSP `connect-src 'self'`), peuple `<datalist id="api-model-list">` (option `vision` étiquetée), mise en cache + garde anti-réponse-périmée ; ollama/mistral gardent leurs datalists live. | **Champ `model` reste libre** (suggestions ≠ contrainte). `interfaces` mince (catalogue en `app`). Comportement navigateur non testable en CI → endpoint testé (TestClient, 3 fournisseurs + inconnu), `node --check`, datalist + référence `/api/models/` asserties. | poursuit 3c ; reste preview normalisation · config save/load · `char_exclude`/profil métrique/expose-ALTO |

| D-089 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3c (3ᵉ sous-tranche) — preview de normalisation (config YAML custom sans persistance)** | Le formulaire offrait le choix d'un profil de normalisation mais **aucun aperçu** de son effet, ni la possibilité d'**essayer une config custom**. | **`formats`** : `NormalizationProfile.from_yaml_text(text)` (parse `safe_load` d'un **texte** YAML, objet attendu) ; `from_yaml(path)` y délègue (DRY). **`app/normalization_preview.py`** (mince) : `preview_normalization(sample, *, profile, config)` — config custom (prioritaire, `from_yaml_text`) > profil nommé (`NORMALIZATION_PROFILES`) > `NormalizationPreviewError` ; **jamais persistée**. Erreurs YAML/Pydantic captées **spécifiquement** (`yaml.YAMLError, ValidationError, ValueError` — pas d'`except Exception`). **`POST /api/normalization/preview`** (routeur engines, **CSRF**, DTO borné) → `{normalized}` ; profil inconnu / config invalide → **422**. **Form** : panneau « Aperçu » (échantillon + `<details>` config YAML custom + bouton + `<pre>` résultat) ; `benchmark.js` poste avec CSRF. CSS `.norm-result` sur la charte. | **Sécurité** : `safe_load` seul, **aucun accès disque**, validation Pydantic (`extra` interdit), borné (4000/8000 car.), CSRF. `interfaces` mince (logique en `app`). Tests : helper (nommé/custom/priorité/inconnu/invalide/ni-l'un-ni-l'autre), endpoint (200/422/403-CSRF), `node --check`, widget + référence API. | poursuit 3c ; reste config save/load · `char_exclude`/profil métrique/expose-ALTO |

| D-090 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3c (4ᵉ sous-tranche) — `char_exclude` câblé du formulaire à la vue** | `EvaluationView.char_exclude` était **dans l'enveloppe** (domain) **et déjà consommé** par le runner (couche 3, symétrique GT/hyp) mais **jamais posé** par `run_planning` (dette « demi-consommé » notée à l'analyse couche 6). | **`run_planning`** : `_ocr_view`/`_reference_view`/`_views_for_corpus`/`plan_benchmark_run` gagnent `char_exclude` → posé sur `EvaluationView.char_exclude`. **`runs` router** : `LaunchRequest.char_exclude` (borné 512) → passé à `plan_benchmark_run`. **Form** : champ « Caractères à exclure » dans les options ; `benchmark.js` l'ajoute au payload du run. i18n FR/EN. | **Pur câblage** (la consommation existait — `evaluation/test_runner` la couvre déjà → pas de re-test redondant). `interfaces` mince. Tests : `char_exclude` atterrit sur **toutes** les vues / absent par défaut · DTO l'accepte · champ + JS asserts. | poursuit 3c ; reste config save/load · profil métrique · toggle expose-ALTO |

| D-091 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3d (1ʳᵉ sous-tranche) — `/metrics` Prometheus opt-in** | Observabilité absente : aucun moyen de suivre le trafic/erreurs d'un Space déployé. | **`interfaces/web/metrics.py`** : `RequestMetrics` (compteur **thread-safe** par `(méthode, statut)`, **cardinalité basse** — jamais le chemin brut ; `render()` Prometheus texte **déterministe**, clés triées) + `MetricsMiddleware` (ASGI pur, capte le statut final). **`create_app(metrics=…)`** + env `XEROCR_METRICS` (`_resolve_metrics`) : si opt-in → middleware (le plus externe, compte même les `429`) + `GET /metrics` (`text/plain; version=0.0.4`). **État par application** (pas de global → factory respectée). | **Opt-in, off par défaut** : un Space exposé ne publie pas ses stats sans décision explicite. **Sans dépendance** (`prometheus_client` non requis — exposition texte stable). Tests : compteur déterministe/format · `/metrics` 404 sans opt-in · compte les requêtes avec opt-in (param **et** env). | poursuit 3d ; reste sélecteur de langue · tooltips/ARIA · feedback dropzone · spinner/progress (vérifier l'existant) |
| D-092 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3d (2ᵉ sous-tranche, **clôt 3d**) — polish a11y (lien d'évitement + `progressbar` ARIA)** | Audit de l'existant : sélecteur de langue (`lang-toggle`, base.html), feedback dropzone (`.is-dragover`, corpus.js), désactivation du bouton + barre de progression + `aria-live` (benchmark) **déjà présents**. Seuls **manquaient** : un **lien d'évitement clavier** (sauter la nav vers le contenu) et l'annonce de **valeur** de la barre de progression aux lecteurs d'écran. | **`base.html`** : `<a class="skip-link" href="#main-content">` en tête de `<body>` + `<main id="main-content" tabindex="-1">` (cible de saut focalisable). **`benchmark.html`** : la barre devient `role="progressbar"` (`aria-valuemin/max/now`, `aria-label`) dans une enveloppe `role="status"`/`aria-live="polite"`. **`benchmark.js`** : `updateProgress` pose `aria-valuenow`. **`shell.css`** : `.skip-link` (hors-écran, visible au focus, sur la charte `--ink`/`--paper`/`--mono`) + `.main:focus{outline:none}`. i18n FR/EN (`skip_to_content`, `bench_progress_label`). | **Aucun changement d'enveloppe** (HTML/CSS/i18n + 1 ligne JS). Charte respectée (jetons de design existants, zéro couleur ajoutée). Tests : lien d'évitement + landmark `#main-content` (FR/EN) · `role="progressbar"` + `aria-value*` + `role="status"`. | **3d ✅** ; prochaine sous-tranche = **3e** (glossaire FR/EN) |
| D-093 | 2026-06-10 | **[PLAN_FIN_MIGRATION.md](PLAN_FIN_MIGRATION.md) Étape 3e (**clôt 3e**) — glossaire pédagogique FR/EN dans le rapport** | Le rapport affichait des métriques (CER/WER/ECE…) sans aucune définition pour le lecteur non-expert. | **DONNÉE** `reports/glossary/{fr,en}.yaml` : une entrée (`title`/`definition`/`measures`/`limits`) par métrique **réellement calculée** (15 : cer, cer_diplo, wer, mer, del/ins_rate, diacritic/mufi_err, hallucination, searchability, region_cer/detection, significance_p, ece, mce) — **jamais en avance sur la donnée**. **Loader** `glossary/__init__` (lecture dynamique du paquet, cache lazy, repli FR, `{}` non bloquant ; `__init__` mince). **`GlossarySection`** : rend, en fin de rapport, les définitions des **seules** métriques présentes dans le `RunResult`, en **disclosure natif `<details>`** (zéro JS, déterministe, autonome). **`SectionContext.lang`** + `ReportRenderer.render(..., lang=)` + `render_document(lang=)` (`<html lang>`) ; **`/reports/{name}?lang=`** (en → glossaire EN) = consommateur réel des deux YAML. CSS au design (monochrome, jetons existants). `package-data` `reports.glossary/*.yaml`. | **Anti-hallucination** : prose 100 % issue de la DONNÉE, aucun LLM. **Anti-spéculation** : entrée ssi métrique avec consommateur. **Autonomie/déterminisme** préservés (HTML/CSS inlinés, octet-stable par langue). Garde-fou archi : `yaml` autorisé en `reports` (`REPORTS_ALLOWED_EXT`, comme formats/app). Tests : couverture FR/EN des 15 métriques · section présente/absente · lang EN · endpoint `?lang=` · déterminisme. | **Étape 3 ✅** (3a→3e livrés) ; prochaine = **Étape 4** (métriques restantes) |
| D-094 | 2026-06-11 | **Décision d'architecture — rapport interactif *et* autonome + vision banc de corpus HF** (design, pas de code) | Constat : le rapport plat actuel (sections empilées + sommaire à ancres) est **loin** de la cible (onglets, galerie à vraies vignettes, drill-in, graphes). Long échange de design pour éviter de figer une mauvaise architecture. | **Deux docs actés** : (1) `xerocr/reports/DECISION_RAPPORT_INTERACTIF.md` — *autonome ≠ statique* : **interactivité disciplinée** (enrichissement progressif, une île de données = `RunResult` inline, **un** script mince CSP-hashé, **pas** la SPA Picarones, **pas** Chart.js → SVG inline) ; **images = références** dans `RunResult`, **jamais les octets** ; **stratégie image par doc** (IIIF / dossier-dérivés / fichier-embarqué / synthétique) ; **saveurs** (fichier unique · dossier · réfs IIIF/HF · servie) choisies à la génération ; **budget d'octets dur** + drill-in texte plafonné (~100-200 pages, pires-d'abord, reste→app web) ; **onglets 4** (Vue/Moteurs/Documents/Croisements) ; **zoom medium + pan/zoom léger, PAS de deep-zoom** ; **correction D-086** (la galerie synthétique était le *fallback* Picarones, pas le design — cible = vraies vignettes). (2) `VISION_DATASET_XEROCR.md` — banc de corpus multi-genres standardisé sur HF, **IIIF statique** (Level 0 + manifestes JSON, pas de serveur), **repro par SHA de Dataset** → `RunManifest`, **droits** : héberger l'ouvert / référencer le restreint ; **spec de standardisation** alignée `domain` = le vrai travail ; bonus = bibliothèque du Space public. | **Revirement assumé** d'une décision documentée (`CLAUDE.md §8` anti « rapport-application ») : on ne revient PAS à la SPA Picarones, on passe à l'interactivité **disciplinée** — consigné, pas en force (pointeur ajouté `CLAUDE.md §6`). **Socle commun à poser d'abord** : `RunResult` porte des **références** image (pas d'octets → déterminisme/hash intacts). Le banc HF = **milestone parallèle** (prérequis droits + spec), pas une feature différée « au cas où ». **Aucun code** dans cet item (design only). | prochaine = cadrer la 1ʳᵉ tranche du rapport interactif (squelette : onglets + 1 graphe SVG + drill-in borné), interne→externe |
| D-095 | 2026-06-11 | **Rapport interactif — tranche 1 (squelette) : coquille à 4 onglets** | Suite D-094 : 1ʳᵉ tranche verticale de pleine profondeur, la plus mince, pour prouver « interactif **et** autonome » avant d'épaissir. | **`renderer.py`** : le sommaire à ancres est remplacé par une **IA 4 onglets** (`_SECTION_TAB` mappe chaque section → Vue d'ensemble/Par moteur/Par document/Croisements ; `glossary` non mappé → rendu après, hors onglets). `_tab_layout` regroupe les sections rendues serveur en **panneaux ARIA** (`role="tablist"`/`tab`/`tabpanel`, `aria-selected`/`aria-controls`) ; sous 2 onglets actifs → pas de barre (empilé). **`report.js`** (+ ~45 lignes, reste épinglé CSP) : bascule un panneau à la fois, roving-tabindex + flèches/Home/End, onglet initial depuis le hash. **`html.py`** : CSS barre pilule au design (`--ink`/`--surface`, `tab-panel[hidden]`). Libellés FR/EN. | **Enrichissement progressif** : le serveur rend **tous** les panneaux ; sans JS ils restent **empilés et visibles** (= le rapport plat d'avant), les onglets sont de simples **ancres** `#panel-<t>`. **Zéro changement d'enveloppe** (sections inchangées, `RunResult` intact), déterministe, autonome. Tests : 4 onglets/panneaux ARIA · regroupement + barre supprimée sous 2 onglets · libellés FR/EN · section non mappée après les panneaux · `report.js` node-check + nav. `make ci` : 1061 passed, 94.95 %. | prochaine = tranche 2 (graphes SVG : dispersion/IC/calibration, convention d'arrondi déterministe) |
| D-096 | 2026-06-11 | **IA du rapport confirmée (option c) + file unique du chantier UI** (design, pas de code) | L'utilisateur a challengé l'IA 4 onglets (« meilleure alternative ? ») ; alternatives examinées (IA par intention, document narratif, synthèse+explorateur, navigation matricielle) — toutes perdent contre l'axe **moteurs/documents** (= les deux axes de la matrice pipelines×documents : drill-ins naturels, modèle mental stable, une maison par donnée). Point faible réel identifié : **« Croisements »** est une annexe statistique — la significativité était lue dans un autre onglet que le verdict qu'elle qualifie (risque de sur-lecture du classement). | **Option (c) validée** : les 4 onglets restent (Croisements a un futur = famille inter-moteurs de l'étape 4e), avec 2 amendements : (1) **significativité inline dans le verdict** (badges sur le classement Vue d'ensemble, livrés en U2) ; (2) **règle de croissance** : *un onglet = une question, une section = une réponse* — chaque famille étape 4 atterrit dans l'onglet de sa question, jamais un onglet par famille. **`xerocr/reports/PLAN_UI_RAPPORT.md` devient LA file unique du chantier rapport** (U1→U2→U3→U4→U5→T3→T4) : il remplace les numérotations antérieures (« tranche 2 SVG » de D-095 ≙ U2 ; « tranches 3/4 » de D-094 ≙ T3/T4). | **Anti-double-comptabilité** : une seule séquence, un seul doc d'autorité pour ce chantier (même rôle que le roll-up pour la migration). `PLAN_FIN_MIGRATION.md` inchangé (étape 4 métriques reste l'autre chantier ouvert ; priorisation entre les deux = décision utilisateur au fil de l'eau). Renommage éventuel de « Croisements » : décision **au contact du contenu** (post-étape 4e), pas d'avance. | prochaine tranche du chantier rapport = **U1** (design system : chrome unifié + héros/cartes + glossaire contextuel) |
| D-097 | 2026-06-11 | **Rapport — U1a : chrome unifié + cartes par section + exports** (1ʳᵉ partie de U1) | Le rapport ne ressemblait pas à la cible `design/` : onglets dans une barre pilule séparée, **une** méga-carte enveloppant tout, pas de méta ni d'exports. | **`html.py`** : `render_document(tabs=, meta=)` — onglets **intégrés au chrome** (barre sombre, actif pilule claire, `--sans` 500, cf. `design/tokens.css`) + bloc méta (`docs/moteurs/date`) + actions ; le corps n'est **plus** enveloppé dans une `.sec` unique. CSS chrome-meta/btn + onglets restylés sombre. **`renderer.py`** : `_tab_layout` → `(barre, corps)` (barre → chrome) ; `_block` → chaque section = **sa** carte `.sec` ; `_chrome_meta` = méta + **exports CSV/JSON** en `data:` URI base64 (`<a download>`, zéro JS, hors-ligne — le JSON embarqué = le `RunResult` complet, la « saveur données » à redonner à un outil tiers). | **Autonomie/déterminisme** : exports `data:` (aucune ressource réseau), octet-stable. **Régression déterminisme corrigée** : le JSON export a **surfacé** les horodatages du manifeste que `demo_to_html` ne neutralisait pas (latence cachée — rien ne les rendait avant) → `demo_to_html` fige désormais `run_id`/`started_at`/`completed_at` (`_DEMO_EPOCH`), comme `usage`/`economics`. Tests : chrome-meta + exports CSV/JSON `data:` · chaque section = `.r-block sec` · plus de méga-carte · `_tab_layout` tuple. `make ci` : 1063 passed, 94.97 %. | reste U1 : **U1b** (héros par vue + headings de section réconciliés) · **U1c** (glossaire contextuel : survol en-têtes + dialog chrome, dépose la section) |
| D-098 | 2026-06-11 | **Rapport — U1b : héros par vue + titres de section réconciliés** (2ᵉ partie de U1) | Sections empilées sans en-tête de vue ; titres de vue dupliqués entre le futur héros et les sections « éponymes ». | **`renderer.py`** : `_hero(tab, num, result, lang)` + `_hero_stats` — bande **héros** par onglet (eyebrow « VUE 0n · NOM » localisé + titre display + desc italique + readouts de portée à droite, **dérivés du `RunResult`**, pas figés) ; injectée en tête de chaque panneau par `_tab_layout(result=…)`. **`html.py`** : CSS `.view-hero*`/`.hero-stat` (carte `--raised`, valeurs `design/tokens.css`). **Dé-duplication** : `overview` perd son `<h2>Vue d'ensemble</h2>` + sa bande de readouts (montés dans le héros) → « Métriques par vue » ; `by_engine` « Par moteur — classement » → « Classement » ; `by_document` perd son `<h2>Par document</h2>` introductif. | **Discipline B2** : le héros est de l'**agencement de vue** (le renderer compose), pas un nouveau contrat — les sections restent le seul `Protocol` de contenu. Stats héros = fonction du `RunResult` (réelles). Strates **non affichées** (pas de donnée → U5). Tests : héros eyebrow/titre/stats · `VUE 01` · overview sans readouts · by_engine « Classement » · by_document « Vue : ». `make ci` : 1064 passed, 94.97 %. | reste U1 : **U1c** (glossaire contextuel : survol en-têtes + `<dialog>` chrome, dépose la section bas-de-page) |

*Prochaines entrées : à ajouter au fil des tranches, dans le même commit que le code.*

---

*Référence : ce plan relie les guides par couche en un parcours de tranches. Enveloppe
(§1) durable ; surface (§3 détail) à confirmer au build. Verdicts de surface marqués
PROVISOIRE. Détail d'avancement = `DoD vivante` de chaque `COUCHE` doc (autorité) ;
ce plan = index + orchestration + journal.*
