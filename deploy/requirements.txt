# Dépendances de la vitrine XerOCR (Space lecture seule).
# Le cœur + l'extra [serve] uniquement — aucun moteur (tesseract/LLM) n'est servi.
# Épinglé pour la reproductibilité du Space (cf. §12) ; relever au besoin.
pydantic>=2,<3
lxml>=5
pyyaml>=6
scipy>=1.11
rapidfuzz>=3,<4
fastapi>=0.115,<1
uvicorn>=0.30,<1
jinja2>=3.1,<4
python-multipart>=0.0.9,<1
# httpx : la vitrine importe les importeurs de corpus (couche 5) → transport HTTP.
# httpcore : moteur de httpx (co-installé) — épinglage d'IP anti-DNS-rebinding.
httpx>=0.27,<1
httpcore>=1,<2
