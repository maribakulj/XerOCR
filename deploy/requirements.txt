# Dépendances de la vitrine XerOCR (Space lecture seule).
# Le cœur + l'extra [serve] uniquement — aucun moteur (tesseract/LLM) n'est servi.
# Épinglé pour la reproductibilité du Space (cf. §12) ; relever au besoin.
pydantic>=2,<3
lxml>=5
pyyaml>=6
scipy>=1.11
fastapi>=0.115,<1
uvicorn>=0.30,<1
