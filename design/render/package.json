{
  "name": "xerocr-design-render",
  "private": true,
  "description": "Offline harness that renders the design report views to reference screenshots (single source of truth = ../tokens.css + ../picarones.css + ../*.jsx).",
  "dependencies": {
    "@babel/standalone": "7.29.0",
    "playwright": "^1.56.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
